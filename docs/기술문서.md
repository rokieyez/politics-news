# 기술 문서

> **출신** — 이 저장소는 부동산 브리핑(estate-news)을 2026-09-08 에 복사해 정치용으로 고친 것입니다.
> 파이프라인 구조는 같고, 부동산 전용이던 실거래가·부동산원 통계(`stats.py`)는 **꺼 두었습니다**
> (`stats.enabled: false`). 그 자리는 국회·여론조사를 직접 세는 `civics.py` 가 맡습니다.
> 정치 뉴스라서 지켜야 할 원칙(편들기 금지·여론조사 인용 표기·의혹 표현)은 `CLAUDE.md` 에 있습니다.

파이프라인 내부 동작과 명령어 설명입니다.
처음 설정하거나 매일 쓰는 방법은 **[README](../README.md)** 를 보세요.

매일 아침 국내 정치 뉴스를 모아서 **네이버 블로그 글 한 편**과 **영상 대본 두 개(쇼츠·롱폼)** 를 만들어 둡니다.

```
RSS 수집 ──▶ 중복 제거 ──▶ 이슈로 묶기 ──▶ 점수 매겨 상위 5개 선정
                                                  │
                                                  ▼
                                          Claude 로 사실 정리
                                                  │
                            ┌─────────────────────┼─────────────────────┐
                            ▼                     ▼                     ▼
                     네이버 블로그 글        쇼츠 대본 + SRT         롱폼 대본
                     (붙여넣기용 HTML)                              + 제작 메모
```

👉 **[예시 산출물 보기](docs/sample-output/)** (가짜 데이터로 만든 샘플)

---

## 무엇이 나오나

하루치 결과는 `output/2026-09-06/` 처럼 날짜 폴더에 쌓입니다.

| 파일 | 용도 |
| --- | --- |
| **`blog-naver.html`** | **네이버 블로그 붙여넣기용.** 브라우저로 열고 버튼만 누르면 됩니다 |
| `blog.md` | 같은 글의 마크다운 판. 보관·검색용 |
| `brief.md` | 오늘의 사실 정리. 이슈별 무슨 일 / 숫자 표 / 왜 중요한지 / **확인 필요 지점** |
| `script-shorts.md` | 60초 쇼츠 대본. 자막 단위로 끊고 컷마다 **화면 지시**가 붙습니다 |
| `script-shorts.srt` | 위 대본의 자막 파일. 프리미어·파이널컷·캡컷에 그대로 임포트 |
| `script-longform.md` | 8분 롱폼 대본. 챕터·타임코드·B-roll·그래픽 지시 포함 |
| `production-notes.md` | 제목/썸네일 문구 후보, 설명란·태그·고정 댓글 복사용, 촬영 체크리스트 |
| `data.json` | 기사에서 뽑은 수치만 모은 것. 차트 만들 때 쓰세요 |
| `img-*.png` | **수치로 자동 생성한 인포그래픽.** 블로그 이미지 자리에 넣으세요 |
| `img-*.svg` | 위 그림의 벡터 원본. 색·문구를 고치려면 이걸 편집 |
| `thumb-longform.png`, `-2.png` / `thumb-shorts.png`, `-2.png` | 롱폼(1280×720)·쇼츠(1080×1920) 표지 1안·2안. 문구 후보 1·2번 |
| `checklist.md` / `.json` | 발행 전 점검표. 사이트 첫 화면 카드의 출처 |
| `sources.md` | 근거 기사 원문 링크 전체 |
| `raw/articles.json` | 수집 원본(기사 본문 포함). 같은 컴퓨터에서 `render` 로 재생성할 때 씁니다. **커밋되지 않습니다** |
| `collect.json` | 수집 건수·피드 상태만 담은 요약. 대시보드가 읽습니다 |
| `policy.md` | **정부 발표 원문 3줄 요약 + 원본 파일 링크** (그날 관련 발표가 있을 때만) |
| `policy/*.pdf` | 위 요약의 근거가 된 보도자료 원본. 사이트의 일괄 내려받기에도 들어갑니다 |
| `civics.md` / `.json` | **국회·여론조사 직접 집계** — 발의 법률안 수, 본회의 표결, 등록된 선거 여론조사, 오늘 이슈에 나온 법안의 현재 단계 |
| `card-*.png` | 유튜브 게시물용 카드뉴스(1080×1080). 그날 본회의 표결이 있으면 표결 카드가 붙습니다 |

주간 결산은 `output/weekly/2026-W36/` 처럼 주차 폴더에 따로 쌓입니다 (`weekly.md`,
`weekly-naver.html`, `data.json`). 매주 일요일 아침 자동으로 만들어지고, `python -m rebrief weekly`
로 언제든 손으로도 만들 수 있습니다.

월간 결산은 `output/monthly/2026-08/` 에 같은 모양으로 쌓입니다 (`monthly.md`,
`monthly-naver.html`, `data.json`). 매달 1일 00:50 UTC 에 **지난달**을 결산합니다 — 깃허브 예약은
`L`(말일)을 못 쓰므로 1일로 잡았습니다. 그달 브리핑이 10일치 미만이면 만들지 않고, 그건 실패가 아니므로
`_cmd_monthly` 가 0 을 돌려줘 워크플로는 성공으로 끝납니다.

### 정부 발표 원문 (`policy.py`)

정책이 나온 날 부처가 내려보내는 보도자료를 찾아 3줄로 줄이고 원본 파일까지 받아 둡니다.

**어디서 가져오나.** 문화체육관광부가 운영하는 **정책브리핑**(korea.kr)의 보도자료 목록입니다.
부처별 누리집을 직접 긁지 않는 이유는 두 가지입니다. 국토교통부 사이트는 자동 접속을 막는 쿠키 검사(TMOSH)에
걸려 프로그램으로는 열리지 않고, 정책브리핑은 여러 부처 발표가 한곳에 모입니다. korea.kr 의 RSS 는 이미
없어져서(404) 목록 화면을 직접 읽습니다.

**추리는 기준.** 정치 키워드(`policy.keywords`)가 제목이나 요약에 있어야 하고, 그 위에 부처
(`policy.departments`) 또는 강한 제목 키워드(국무회의·개각·재의요구 등, `policy.STRONG`) 중 하나를 만족해야
합니다. 부처 이름만으로 거르면 행안부의 재난 안내까지 딸려 오고, 키워드만으로 거르면 지자체 홍보가 섞입니다.
한 화면에 20건뿐이라 며칠치를 보려면 `policy.list_pages`(기본 4장)만큼 넘깁니다.

**본문은 첨부 PDF 에서 뽑습니다.** 상세 화면의 본문 영역에는 "…전재하여 제공함을 알려드립니다" 같은 안내문만
있고 실제 내용은 문서 뷰어(iframe) 안에 있습니다. 그래서 첨부 파일을 받아 `pypdf` 로 글자를 읽습니다.
120자 미만이거나 '전재하여' 가 든 본문은 본문으로 치지 않고 버립니다.

**첨부는 PDF 를 먼저.** 같은 문서가 hwp·hwpx·pdf 세 벌로 올라오므로 확장자 우선순위(pdf → hwpx → hwp)로
정렬해 `policy.max_files_per_doc`(기본 1)개만 받습니다. 파일명은 한글·영숫자만 남기고 80자로 자릅니다 —
괄호와 공백이 섞인 원래 이름은 마크다운 링크에서 깨집니다. 받지 않은 나머지 파일은 정책브리핑 원본 링크로 답니다.

**요약은 3줄.** `llm.policy_model`(기본 haiku)이 1줄 무엇/2줄 숫자/3줄 영향으로 씁니다. 프롬프트가 문서에 없는
사실을 더하지 못하게 묶여 있고, 완전한 문장을 요구합니다. 키가 없거나 호출이 실패하면 문서에서 그대로 떼어 온
`extractive_summary` 로 대신합니다 — 부처 요약문의 □·◇·ㅇ 표시를 기준으로 문단을 자릅니다.

### 0원 방식 — 모델 대신 채팅 붙여넣기 (`answer.py`, 2026-09-10)

사용자가 API 요금(하루 약 $0.5)이 부담된다고 해서 고른 방식입니다. 아침 데일리는 `llm.enabled: false` 라 기사 수집·
국회 집계·붙여넣기 묶음까지만 만듭니다(0원). 그다음은 사람이 두 번 붙여 넣습니다.

1. `prompts.build_prompt_pack` — 애널리스트 규칙 + 오늘 기사(발췌 500자, 공개 저장소에 올라가므로 본문 전문은 안 넣음) +
   블로그·대본 지시 + **JSON 스키마 세 개**를 한 문서로. 답은 ```json brief / blog / script 블록 셋이어야 한다고 못박습니다.
   `prompt-pack.md`(저장용)와 `prompt-pack.html`(휴대폰에서 「묶음 복사」 버튼, 이슈 새로 만들기 링크)로 나옵니다.
2. `answer.save_pack` — 답을 읽을 러너에는 기사 원본(`raw/`)이 없으므로 `pack.json` 에 기사 목록을 남깁니다. 본문 대신
   본문의 **숫자 토막**만 남겨 검산(`verify`)이 돌게 합니다. 제목·주소·매체·RSS 요약은 `sources.md` 와 같은 수준의 공개 자료입니다.
3. 사용자가 답을 「답 YYYY-MM-DD」 이슈에 붙여 넣으면 `.github/workflows/answer.yml` 이 본문을 파일로 받아
   `python -m rebrief answer` 를 돌립니다. `answer.parse_answer` 가 블록을 찾아 `DailyBrief`·`BlogPost`·`VideoPack` 으로
   검증하고(표식이 빠졌거나 한 객체로 묶여 와도 읽음), `pipeline.answer` 가 API 경로와 **같은 산출 단계**
   (`_render_brief_part` → `_render_blog_part` → `_render_video_part`)를 탑니다. 브리핑 덩이만 있으면 브리핑·카드까지,
   블로그가 있으면 글까지, 대본이 있으면 대본·썸네일까지. 형식이 틀리면 이슈에 어느 칸이 틀렸는지 댓글을 달고 열어 둡니다.
4. 성공하면 `output/` 을 커밋하고 이슈를 닫습니다. 「사이트 배포」가 이어서 돌아 사이트에 올라갑니다.

점검표의 「요약 없음」은 이제 실패(❌)가 아니라 다음 단계 안내(⚠️)입니다. 비용 장부에는 아무것도 안 남습니다.

### 국회·여론조사 직접 세기 (`civics.py`)

기사에 실린 숫자가 아니라 **원자료를 직접 센 값**입니다. 두 곳을 읽습니다.

**열린국회정보 (open.assembly.go.kr).** 국회의원 발의법률안(`nzmimeepazxkubdpn`)과 본회의 처리안건
(`nwbpacrgavhjryiph`). `ASSEMBLY_API_KEY` 가 없으면 **견본 모드**라 한 번에 5행만 오지만 머리의
`list_total_count` 는 맞습니다. 그래서 키가 없으면 기간 안의 건수는 세지 않고(`count=None`) 최근 5건만
보여 줍니다 — 5건을 '이번 주 5건' 으로 적으면 거짓말입니다. 키를 넣으면 쪽을 넘겨 셉니다. 발의안 목록은
최신순이라 날짜가 지나면 멈추고, 본회의 처리안건은 의안번호 순이라 끝까지 넘깁니다(1,700여 건 = 18쪽).
`KEY=sample` 같은 가짜 키는 ERROR-290 으로 거부되므로 키가 없으면 `KEY` 를 아예 빼고 부릅니다.

**깃허브 러너(해외)에서는 이 사이트 접속이 흔들립니다** (2026-09-09 실측: 같은 날 어느 때는 5회 모두 1초, 아침
데일리는 세 번 다 20초 ConnectTimeout). 그래서 두 겹으로 막습니다.

* **맥에서 미리 받기.** 아침 깨우미(`scripts/wake-daily-brief.sh`)가 워크플로를 부르기 전에 맥(한국 IP)에서
  `python -m rebrief civics --prefetch` 를 돌려 `state/civics/<날짜>.json` 을 저장소에 밀어 넣습니다. 그날 기사
  제목에 나온 법안 이름도 미리 찾아 둡니다(`tracked_pool`). 러너는 이 파일이 있고 견본이 아니면 국회에 묻지
  않고, 견본이면 직접 받되 못 받은 항목만 파일에서 채웁니다(`merge_prefetch`). 맥의 `.env` 에
  `ASSEMBLY_API_KEY` 가 있어야 견본이 아닙니다.
* **낮 보충.** 「국회 자료 보충」 워크플로(12:00 KST 예약, 실제로는 두 시간쯤 늦게)가 아침에 못 받은 항목을
  다시 받아 `civics.md`·`json` 만 채웁니다(`python -m rebrief civics --refresh`). 이미 완전하면 아무것도 안 합니다.
  모델은 부르지 않습니다.

**중앙선거여론조사심의위원회 (nesdc.go.kr) 등록부.** 인증키가 없습니다. 목록 화면은 `<a class="row tr">`
안에 `<span class="col">` 여덟 칸(등록번호·기관·의뢰자·방법·추출틀·명칭·등록일·시도)이고, 상세 화면의
th/td 짝에서 조사일시·사례수·응답률·표본오차·공표 매체를 읽습니다. `Poll.summary()` 가 공직선거법
108조가 요구하는 항목을 한 줄로 만듭니다 — 「여론조사공정(주) · 펜앤마이크 의뢰 · 2026-09-06~07 · 1,015명 ·
무선 ARS · 응답률 2.1% · 95% 신뢰수준에 ±3.1%p」.

**오늘 이슈에 나온 법안은 지금 어디에 (`track_issue_bills`, 2026-09-08).** 브리핑 이슈의 제목·한 줄·
사실 항목에서 '…법' 으로 끝나는 낱말을 뽑고(`law_names` — 후속입법·방법 같은 것은 `NOT_A_LAW_SUFFIX` 로
거릅니다), `BILL_NAME` 부분 일치로 찾아 가장 최근 계류 안건의 단계를 적습니다(`bill_stage` — 소관위 회부 →
상정 → 처리 → 법사위 → 본회의 결과 순으로 첫 값이 있는 열). 브리핑이 나온 **뒤에** 돌기 때문에 `civics.md`
를 한 번 더 씁니다. 모델은 부르지 않습니다.

**어디에 들어가나.** `civics.md`(사이트 '국회·여론조사 집계'), 블로그 끝의 '직접 센 숫자' 상자
(`render.civics_block_html`), 영상 대본의 user 프롬프트(`prompts.civics_context`), 그리고 **본회의 표결
카드**(`images.pick_vote` / `_vote_card` — 실행일과 전날의 표결 가운데 이슈 제목과 낱말이 겹치는 법안, 없으면
표가 가장 많은 법안 하나). 한 곳이 죽어도 다른 곳 값은 남고, 둘 다 죽으면 `{}` 라 글은 그대로 나갑니다.

**점검표와 이어집니다.** `checklist.poll_citations` 가 여론조사 수치 옆에 조사 개요가 빠졌는지 보고,
글이 스스로 "조사 개요가 자료에 없다" 고 밝힌 문장이 있으면(`poll_self_disclosed`) 경고하지 않습니다.
`election_blackout` 은 `elections:` 달력의 선거일 전 6일부터 새 조사 수치를 막고, `side_balance` /
`quote_balance` 는 두 진영의 언급 수와 **발언 인용 수**를 따로 셉니다 — 언급은 고른데 옮긴 말이 한쪽뿐인
글을 잡기 위해서입니다.

시험은 `tests/test_civics.py`, 망은 `conftest` 가 `civics.enabled=False` 로 끕니다.
**Jinja 에서 `plenary.items` 는 dict 메서드에 잡히므로** 템플릿은 `plenary["items"]` 로 씁니다.

**꺼 둔 실거래가·부동산원 통계 (`stats.py`).** 코드는 남아 있고 `stats.enabled: false` 라 돌지 않습니다.
다시 켜야 할 일이 생기면 `../estate-news/docs/기술문서.md` 의 같은 절을 보세요.

### 인물 조사 (`profile.py`, 2026-09-09)

`python -m rebrief profile "이름" [--birth 1964] [--pack] [--llm] [--date …]`. 데일리 파이프라인과는 별개입니다.
**기본은 모델 없이 정리표만** — 자료를 모아 `profile_tables.md.j2` 로 표(기본 정보·선거 이력·정당 이력·공식 약력·발의 건수·
연도별 기사 제목)를 그립니다(`render_tables`). `--pack` 은 자료묶음(prompt-pack. 지시문 + 번호 붙은 자료 전부), `--llm` 은
모델을 **한 번** 불러 `PersonProfile`(연표·장·논란·최근·구성안·제목 후보·주의·미상)을 받아 `profile.md.j2` 로 그립니다.
산출물은 `output/profiles/이름-생년/` 에 `이름(생년,정당)_날짜_정리표.md` · `이름(생년,정당)_날짜.md`(자료묶음) ·
`이름(생년,정당)_날짜_AI정리.md` · `sources.json`(원자료) · `profile.json`(모델 출력). 이름 규칙은 `file_base` — 생년·정당을
모르면 그 칸을 뺍니다. 비용은 `costs.json` 에 `kind=profile` 로(`record` 뒤 `save()` 를 불러야 남습니다 — 처음에 빠뜨렸음).

워크플로 `profile.yml` 이 `workflow_dispatch` 입력(name·birth·pack·ai)으로 같은 명령을 돌리고 `output/profiles` 를
커밋합니다. **rokiz.net/politics** (집 저장소 `rokieyez.github.io/politics/index.html`)가 이 워크플로를 GitHub API 로
부르는 버튼 페이지입니다 — fine-grained 토큰(politics-news 의 Actions: Read and write)을 브라우저 localStorage 에만 두고,
결과 목록은 `git/trees/main?recursive=1` 한 번으로 읽어 raw.githubusercontent 에서 받아 파일로 떨어뜨립니다. 서버는 없습니다.

**출처 넷 — 전부 인증키 없이 돕니다 (2026-09-09 실측).**

* **열린국회정보 역대 국회의원 인적사항 `ALLNAMEMBER`** — `NAAS_NM=이름` 으로 부르면 키 없이도 그 사람 행이
  통째로 옵니다(견본 5건 제한은 목록에만 해당). `PLPT_NM`·`ELECD_NM`·`ELECD_DIV_NM` 은 대수 순서로 `/` 로 이어진
  문자열이라 `_split` 으로 나눠 `GTELT_ERACO`("제15대, 제16대, …")와 짝을 맞춥니다. `BRF_HST` 가 공식 약력(학력·경력,
  `\r\n`). 국회의원을 지낸 적 없는 사람(황교안)은 `RESULT.CODE=INFO-200` 이라 빈 목록으로 처리합니다.
  동명이인은 행이 여러 개(이재명 2명) → `--birth` 로 고르고, 없으면 최근 대수 사람을 골라 나머지를 `notes` 로 알립니다.
* **발의법률안 `nzmimeepazxkubdpn`** — `AGE`·`PROPOSER=이름`. 키 없이도 `list_total_count` 는 정확하고 행은 5건.
  자료가 있는 제20대부터, 최근 3개 대수만 부릅니다(`BILLS_MIN_AGE`).
* **위키백과** — `list=search "이름 정치인"` 후보 → `exintro` 머리글에 이름·'정치'·(알면) 생년이 있는 첫 문서를
  고릅니다(`_is_this_person`). 본문은 `prop=extracts&explaintext` 로 받아 `== 절 ==` 로 나누고, 같이 보기·외부 링크·
  각주는 버립니다. **표로만 된 절(역대 선거 결과·소속 정당)은 평문 추출에서 비어 버리므로** `action=parse` 로
  위키텍스트를 따로 받아 `{{선거기록/KR/개인|…}}` 틀과 wikitable 행을 `칸 · 칸 · 칸` 줄로 풉니다
  (`wikitext_table_lines`). 낙선 기록은 여기에만 있습니다. CC BY-SA 라 정리 글 끝에 출처 문구를 넣습니다.
* **구글뉴스 검색 RSS** — `"이름" (의원 OR 정치 OR …)` 로 동명이인 잡음을 줄이고, `after:`/`before:` 로 시기를
  자릅니다. 2001년 기사까지 옵니다. 시작 연도는 첫 당선 대수 1년 전(`AGE_START_YEAR`) → 위키 머리글의 첫 연도 →
  12년 전 순으로 잡고(`career_start_year`), `plan_windows` 가 창을 10개 이내로 맞춥니다. 호출 사이 0.6초 쉽니다.
  제목의 ` - 매체` 꼬리를 떼고, 본문은 긁지 않습니다.

**깃허브 러너에서는 열린국회정보 접속이 자주 끊깁니다 (2026-09-09 실측: 세 번 중 두 번 ConnectTimeout).** 완전 차단은 아니라
`_get` 이 연결 실패만 한 번 더 시도하고(`civics._get` 도 같이), 그래도 못 받으면 [A]·[B] 없이 갑니다. 그래서 `collect` 는 '못 받음' 과 '없음' 을
갈라 적고, `wiki_basics` 가 위키 표에서 생년(머리글의 `YYYY년 M월 D일`)·최근 정당(소속 정당 표 마지막 행)·당선(선거 결과 표의
'당선' 행)을 뽑아 정리표의 기본 정보와 파일 이름(`file_base`)·폴더 이름(`folder_name`)을 채웁니다. 맥에서 돌리면 [A] 가 붙습니다.

**프롬프트(`PROFILE_SYSTEM`)가 데일리와 다른 점.** 전기는 모델이 기억으로 쓰기 쉬운 글이라 (1) 문장마다 자료
번호를 달게 하고 (2) [W] 에만 있는 사실은 `cautions` 에 "위키백과에만 있음" 으로 분리하며 (3) 논란은
의혹·해명·현재 상태 세 칸을 강제합니다. 기본 모델은 `llm.model`(강한 쪽), `profile.model` 로 바꿉니다.
`profile.checks` 가 금지 표현(`video.banned_phrases`)과 해명 없는 논란을 정리 글 끝 '발행 전 점검' 에 올립니다.

시험은 `tests/test_profile.py` — 세 출처 응답을 붙박이로 두고 `profile._get` 만 갈아끼웁니다.

### 공유 카드·구독 피드 (`site.meta_tags` / `_build_feed`)

`site.url` 이 설정돼 있을 때만 만듭니다. 카드 이미지는 상대 주소를 받아 주지 않는 곳이 많아
전체 주소로만 넣고, 크기(`og:image:width/height`)는 실제 파일 크기를 적습니다 — 다르게 적으면
카드가 잘립니다. 구독 피드의 날짜는 `strftime` 을 쓰지 않고 요일·달 이름을 직접 적습니다.
컴퓨터 언어 설정이 한국어면 `%a` 가 한글을 내보내 구독기가 못 읽기 때문입니다.
피드가 거는 주소는 붙여넣기용 화면이 아니라 읽기용 `brief.html` 입니다.

### 정책 후속 발표와 시행일 (`policy.schedule_items` / `store.PolicyLog`)

보도자료에서 앞으로의 일정만 뽑습니다. 날짜만 뽑으면 통계표의 숫자까지 걸리므로,
**날짜가 든 문장 안에 일정을 뜻하는 낱말**(시행·입법예고·접수·마감 등)이 있을 때만 싣습니다.
문장 경계는 부처 문서의 □·ㅇ 표시와 '~다.' 로 잡습니다. 기간(9.15~10.24)은 마감일 하나로 줄입니다.
`state/policies.json` 에 지금까지의 발표를 남겨 두고 제목 Dice 유사도 0.45 이상이면 같은 정책의
후속으로 이어 붙입니다. 장부는 120건까지만 남깁니다.

### 인포그래픽은 어떻게 골라지나

`data.json` 의 수치를 훑어 **생김새에 맞는 그림 형태**를 고릅니다. 주제가 매일
바뀌므로 특정 주제에 고정돼 있지 않고, 맞는 게 없으면 만들지 않습니다.

| 형태 | 언제 나오나 |
| --- | --- |
| 서울 자치구 도식 | 수치가 '자치구 N개' 이고 설명에 '가·나·다 제외' 가 있을 때 |
| 지수 비교 막대 | 증감률(%)일 때. 구간별 실제 값이 없으므로 100 기준 지수로만 그립니다 |
| 수치 카드 | 그 외 모든 수치의 기본형 |

정보량이 많은 형태를 먼저 채우고 남는 자리를 카드로 메웁니다. 장수는
`settings.yaml` 의 `images.max` (기본 3장) 를 따릅니다.

**없는 숫자는 그리지 않습니다.** 자치구 도식은 25칸에서 제외분을 뺀 수가 보도된
값과 다르면 아예 그리지 않고, 지수 막대는 각주에 '구간별 실제 수치는 보도에
제시되지 않았다' 고 밝힙니다.

PNG 변환은 크롬(또는 크로미엄)이 설치돼 있을 때만 덤으로 합니다. 없으면 SVG 만
남고 실행은 그대로 성공합니다. 네이버 블로그에는 SVG 를 올릴 수 없으니 PNG 를
쓰세요.

### 그림이 본문 자리에 어떻게 들어가나

블로그 글을 만들 때 모델이 `[이미지: …]` 자리마다 "이 자리는 어떤 수치의 그림인지"
(`image_slots[].datapoint_label`)를 함께 적어 줍니다. 프로그램은 그 라벨로 수치를 찾아
그림을 만들고, 파일명에 자리 번호를 붙입니다 (`img-1-district-map.png` = 1번 자리).

- `blog.md` 에는 실제 이미지 문법(`![…](img-1-….png)`)으로 들어갑니다.
- `blog-naver.html` 에는 그 자리가 **초록 상자**로 바뀌고 파일명이 적히며, 아래에 미리보기가
  붙습니다. 미리보기는 복사에 포함되지 않습니다 — 붙여넣기에 상대 경로 `<img>` 가 섞이면
  네이버 에디터에 깨진 그림이 생기기 때문입니다.
- 라벨이 비었거나(사진 자리) 맞는 수치가 없으면 점선 상자 그대로 둡니다.

### 추이 그래프는 언제 나오나

매일 뽑힌 수치를 `state/datapoints.json` 에 쌓습니다. 같은 지표(단위가 같고 라벨이
0.6 이상 비슷한 것)가 **사흘 이상** 쌓이면 꺾은선 추이가 다른 형태보다 먼저 선택됩니다.
첫날부터 며칠은 아무것도 안 나오는 게 정상입니다.

### 시계열·비용·링크 장부

| 파일 | 내용 |
| --- | --- |
| `state/datapoints.json` | 날짜별 수치 이력. 추이 그래프의 재료 |
| `state/costs.json` | 실행마다 쓴 토큰과 비용. `output/INDEX.md` 아래 **비용 (실측)** 절로 요약됨 |
| `sources.md` 상단 | 선정된 이슈의 기사 링크를 HEAD 요청으로 점검한 결과. 죽은 링크는 ⚠️ |

### 발행 전 점검표 (`checklist.py`)

실행 끝에 `checklist.md` / `checklist.json` 을 만듭니다. 항목: 숫자 검산 미확인, 죽은 링크,
**금지 표현 실제 검사**(블로그·쇼츠·롱폼 텍스트에서 `video.banned_phrases` 를 찾음), 블로그
분량(설정의 ±20%), 태그 수, 빈 사진 자리, 쇼츠·롱폼 발화량(cpm 기준), 실행 경고. 사이트 첫
화면의 카드는 `checklist.json` 을 읽습니다. LLM 을 못 쓴 날은 ❌ 한 줄만 남습니다.

### 금지 표현 자동 수정 (`checklist.autofix_model`)

점검표가 ❌ 금지 표현을 찾으면 그 **문장만** 잘라 저렴한 모델(`claude-haiku-4-5`)에 "같은 뜻으로,
표현 빼고" 다시 쓰게 하고 해당 산출물(블로그·쇼츠·롱폼)을 다시 씁니다. 고친 뒤에도 표현이
남으면 바꾸지 않고 ❌ 로 둡니다. 전후 문장은 `checklist.md` 맨 위 항목에 남습니다. 비용은 그날
장부에 합산됩니다. 모델을 비우면 끕니다.

### 연속 실패

`store.failure_streak()` 이 오늘부터 거꾸로 `data.json` 없는 날을 셉니다. 2일 이상이면 실패
알림 문구가 "원인 점검"으로 바뀌고 `dashboard.html` 맨 위에 빨간 띠가 뜹니다.

### 주간 결산의 추이 그림

`state/datapoints.json` 에서 그 주 안에 사흘 이상 나온 지표를 골라 최대 2장(`img-time-series-N.png`).
사이트의 주간 폴더에 같이 복사됩니다.

### PWA

`site/manifest.webmanifest` + `icon.svg`(크롬이 있으면 `icon-512.png` 도). 서비스워커는 일부러
두지 않았습니다 — 매일 바뀌는 페이지에 캐시가 남으면 어제 글을 보게 됩니다.

### 안전망 실행 · 월 예산

- 데일리 워크플로에 07:25 KST 크론이 하나 더 있습니다. 첫 단계에서 `output/<오늘>/data.json`
  이 이미 있으면 나머지 단계를 전부 건너뜁니다 (`steps.guard.outputs.skip`). 수동 실행은 항상 돕니다.
- `llm.monthly_budget_usd` > 0 이면 `state/costs.json` 의 이번 달 합계로 판단합니다.
  `budget_soft_ratio`(기본 0.8) 이상이면 `fallback_model` 로, 예산 이상이면 요약을 건너뜁니다.

### 어제와 달라진 점

브리핑 상단에 직전 날짜 `data.json` 과 이슈 제목을 유사도 0.35 로 견줘 🆕 새 이슈 / ↪️ 이어지는
이슈 / ⏹ 빠진 이슈를 적습니다. LLM 호출 없음.

### 짧게 읽히는 글 (2026-09-07)

| 무엇 | 어디 |
| --- | --- |
| 메인 1개 + '그 밖의 오늘 소식' 한 줄씩 | 프롬프트 규칙. `checklist` 의 `shape` 항목이 확인 |
| 분량 1,200~2,000자 | `blog.min_chars` / `max_chars` |
| 낯선 말 풀이 | `glossary.py` 의 고정 사전 40개. 글에 나온 순서로 `blog.glossary_max` 개 |
| 그래서 나는? | `BlogPost.takeaways`. 글 끝 상자 |

용어 풀이를 모델에 맡기지 않는 이유: 같은 말을 날마다 다르게 설명하면 독자가 헷갈립니다.
사전은 사람이 적어 둔 고정 문장이고, 뜻이 같은 말(필리버스터/무제한 토론)은 한 번만 나옵니다.

### 그림의 공통 카드 틀 (`images.frame_open`)

모든 인포그래픽이 같은 틀을 씁니다: 흰 카드 + 머리말(채널·날짜) + 제목·부제 + 가는 구분선 + 각주.
낱장으로 보나 여러 장을 나란히 보나 같은 서식이라 자료처럼 읽힙니다.
카드 높이는 `card_height(w, 제목, 부제, 본문높이, 각주)` 로 미리 계산합니다 — 고정 높이로 두면
각주가 큰 숫자와 겹칩니다(실제로 그랬습니다). 축 눈금은 `nice_ticks` 가 1·2·5 배수로 떨어뜨리고
데이터 범위를 반드시 덮습니다.

### 첨부파일 zip (`site._build_zip`)

날짜 폴더마다 `files.zip` 을 미리 만들어 둡니다(그림·썸네일·SRT·컷 리스트). 브라우저에서
자바스크립트로 묶지 않는 이유는 휴대폰에서 확실히 받아지게 하려는 것입니다.

### 검색 유입 장치 (2026-09-07)

| 무엇 | 어디 | 검사 |
| --- | --- | --- |
| 대표 검색어 `focus_keyword` | 제목 앞 15자·첫 120자·소제목 | `checklist.keyword_placement` 가 빠진 자리를 ⚠️ 로 |
| 3줄 요약 `summary_lines` | 글 맨 앞 파란 상자 | — |
| 목차 | 본문 `##` 에서 자동 생성 (소제목 3개 이상일 때) | — |
| 마무리 질문 `closing_question` | 글 끝 | — |
| 지난 글 링크 | 글 끝 (`related.related_posts`) | 발행 기록에 주소가 있는 날만 |
| 지난 글과 문장 겹침 | — | `checklist.overlap_with_previous`, `blog.max_overlap_ratio` |

목차에 앵커 링크를 걸지 않는 이유: 네이버 에디터에 붙여넣으면 id 가 사라져 링크가 죽습니다.
지난 글은 오늘 이슈 제목과 그날 이슈 제목의 Dice 유사도로 고르고, 겹치는 주제가 없으면 최근 순입니다.

### 이슈 선정 근거 (`render.explain_issues`)

이슈마다 '오늘 2위 · 매체 5곳 · 기사 7건 · 최신 09-06 22:05' 한 줄이 `brief.md` 에 붙습니다.
모델을 부르지 않고 수집 결과만 셉니다. 이슈와 묶음은 **근거 기사 주소가 겹치는 것**으로 맞춥니다 —
모델이 순서를 바꾸거나 제목을 달리 붙여도 주소는 그대로이기 때문입니다.

### 같은 주제 반복 (`pipeline._repeat_topics`)

최근 `run.repeat_lookback_days` 일치 `data.json` 의 이슈 제목과 오늘 제목을 Dice 0.5 로 견줍니다.
겹치면 점검표에 ⚠️ 로 올리고 그때 제목을 같이 보여 줍니다. 기사 단위 재탕 방지(`skip_recent_days`)와
달리 **주제 단위**로 봅니다.

### 자막 줄바꿈과 컷 리스트

`render.wrap_caption` 이 자막 한 컷을 `video.caption_max_chars`(16자)·`caption_max_lines`(2줄)로 끊습니다.
그 폭으로 안 담기면 담길 때까지 폭을 한 글자씩 넓힙니다 — 글자를 버리지 않으면서 줄 길이를 고르게 하려는 것.
두 줄로도 넘치는 컷은 점검표에 올립니다. `shorts-cuts.csv`·`longform-chapters.csv` 는 타임코드를
`HH:MM:SS:FF`(`video.cut_list_fps`)로 적고, 엑셀 한글이 깨지지 않게 BOM 을 붙입니다.

### 이번 주 볼 것 (`site/upcoming.html`)

최근 7일 `data.json` 의 `tomorrow_watch` 를 모아 Dice 0.7 이상이면 같은 말로 보고 한 번만 싣습니다.

### 발행 기록 (`state/published.json`)

`publish` 명령이나 **발행 기록** 워크플로로 날짜·주소·조회수를 적으면 사이트 첫 화면에 표시됩니다.

### 지난 글 검색 (`site/search.html`)

빌드 때 모든 날의 `data.json` 을 `search-index.json` 하나로 모으고, 페이지 안 자바스크립트가
제목·한 줄 요약·수치 이름에서 부분 일치로 찾습니다. 서버가 없으므로 GitHub Pages 에서 그대로 됩니다.

### 숫자 검산 (`verify.py`)

브리핑의 각 수치 값을 정규화(콤마·공백 제거, 부호·범위 표기 허용)해서 그 이슈의
근거 기사 제목+본문에 글자 그대로 있는지 봅니다. 상태는 **확인 / 미확인 / 대조 불가**
(본문을 수집하지 못한 기사). LLM 호출은 없습니다. 미확인이 있으면 실행 경고에도 올라갑니다.

### 모델 강등 (`llm.fallback_model`)

한도(429)·과부하(529)·5xx·연결 오류면 대체 모델로 한 번 더 시도합니다. 인증 오류나
요청 거부(4xx)는 강등하지 않습니다. 강등된 호출은 결과 경고에 적히고, 비용은 호출마다
실제 쓴 모델 단가로 계산됩니다 (`Usage.models_used`).

### 제목 기록장 (`state/titles.json`)

실행마다 블로그·롱폼·쇼츠 제목 후보가 저장됩니다. `python -m rebrief titles log` 또는
**제목 기록** 워크플로로 고른 번호와 조회수를 적으면 INDEX 와 대시보드에 유형별 평균이 나옵니다.

### 주간 결산의 반복 이슈

7일치 이슈 제목을 유사도 0.35 로 묶어 이틀 이상 나온 이슈에 `days_seen` 을 달고,
프롬프트에 "여러 날 반복된 이슈" 목록을 넘깁니다. 같은 사건을 세 번 세지 않게 하기 위해서입니다.

### 운영 현황 (`site/dashboard.html`)

최근 30일의 기사 수·피드 성공률·요약 성공/실패·그림 장수·비용을 한 장에 모읍니다.
사이트 첫 화면 아래 링크.

### 알림

`TELEGRAM_BOT_TOKEN` 과 `TELEGRAM_CHAT_ID` 가 있으면(`.env` 또는 깃허브 Secrets) 실행이
끝날 때 헤드라인·이슈 수·그림 장수·사이트 링크를 한 번에 보냅니다. Actions 가 실패하면
실패 알림이 따로 갑니다. 없으면 아무 일도 하지 않습니다.

`python -m rebrief site` 를 돌리면 위 결과가 `site/` 아래 휴대폰용 HTML 로 변환됩니다.
`site/latest/` 는 항상 최신 날짜의 사본이라 즐겨찾기 주소가 바뀌지 않습니다.
GitHub Pages 배포는 `.github/workflows/pages.yml` 이 자동으로 합니다.

이 저장소에는 개인 도메인 `www.rokiz.net` 이 Pages 커스텀 도메인으로 연결돼
있어 산출물은 `https://www.rokiz.net/politics-news/` 로 서비스됩니다.
(`https://rokieyez.github.io/politics-news/` 도 동일하게 열립니다.)
커스텀 도메인은 저장소 Settings → Pages 에 저장돼 있고 리포지터리에 CNAME
파일은 두지 않습니다 — `build_site()` 가 `site/` 를 매번 지우고 다시 만들기
때문에 CNAME 파일을 두면 배포 때마다 사라집니다.

대본 분량은 목표 길이에 맞춰 글자 수로 계산합니다. 기본값은 분당 330자(한국어 낭독 속도)이고,
말이 빠르거나 느리면 `config/settings.yaml` 의 `speaking_rate_cpm` 을 조절하세요.

---

## 시작하기 — 두 가지 방법

### 방법 A. 설치 없이 쓰기 (권장)

**내 PC에 아무것도 안 깔아도 됩니다.** GitHub이 매일 알아서 돌리고 결과를 저장소에 올려 줍니다.
단계별 안내는 [README](../README.md#처음-한-번만-하는-설정) 에 있습니다.

1. 저장소 → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**
   - Name: `ANTHROPIC_API_KEY`
   - Secret: [console.anthropic.com](https://console.anthropic.com) 에서 발급받은 키
2. **Settings** → **Actions** → **General** → 맨 아래 Workflow permissions →
   **Read and write permissions** 선택 후 저장
3. **Actions** 탭 → **피드 점검** → **Run workflow** → 뉴스 피드가 살아있는지 확인
4. **Actions** 탭 → **정치 데일리 브리핑** → **Run workflow** → 첫 실행

이후로는 **매일 06:45(KST)** 자동으로 돌아 7시 전에 준비되고, 결과가 `output/` 폴더에 커밋됩니다.
실행 결과는 Actions 실행 화면에서 바로 읽을 수 있습니다(브리핑 전문이 요약란에 나옵니다).

시간을 바꾸려면 `.github/workflows/daily-brief.yml` 의 cron 을 고칩니다.
UTC 기준이라 **한국 시간에서 9시간을 뺀 값**을 씁니다.

```yaml
- cron: "45 21 * * *"   # 21:45 UTC = 06:45 KST (정각은 깃허브가 밀리거나 빠뜨릴 수 있어 피함)
- cron: "0 20 * * *"   # 20:00 UTC = 오전 5시 KST
```

### 방법 B. 내 PC에서 돌리기

결과를 즉시 보고 설정을 이것저것 바꿔 보고 싶을 때 씁니다.

```bash
git clone https://github.com/rokieyez/politics-news.git
cd politics-news
```

**macOS** — 터미널에서:

```bash
bash setup.sh
```

**Windows** — 파일 탐색기에서 `setup.bat` 을 더블클릭.

파이썬 확인 → 가상환경 생성 → 패키지 설치 → 피드 점검까지 한 번에 합니다.
파이썬이 없으면 어디서 받아야 하는지 알려 줍니다.

설치가 끝나면:

```bash
source .venv/bin/activate      # Windows: .venv\Scripts\activate
cp .env.example .env           # .env 에 ANTHROPIC_API_KEY 입력
python -m rebrief run
```

> **API 키가 없어도 됩니다.** 수집·중복제거·이슈 선정까지는 그대로 돌아가고,
> 요약과 대본은 `prompt-pack.md` 라는 파일로 나옵니다. 그걸 챗봇에 붙여넣으면 같은 결과를 얻습니다.

---

## 네이버 블로그에 올리기

네이버 스마트에디터는 마크다운을 못 읽습니다. `##` 같은 기호가 본문에 그대로 보이죠.
그래서 **서식이 살아있는 HTML** 을 따로 만들어 둡니다.

1. `output/<날짜>/blog-naver.html` 을 **브라우저로 엽니다** (더블클릭)
2. 위쪽 초록 바에서 **제목 복사** → 네이버 글쓰기 제목 칸에 붙여넣기
3. **본문 복사** → 본문 칸에 붙여넣기 — 소제목·표·굵게·목록이 그대로 살아납니다.
   `keynumbers.pick` 이 고른 핵심 수치(`blog.key_numbers`)는 맨 위 '오늘의 숫자' 표 + 본문 첫 등장 굵은 형광펜,
   `blog.highlight_repeats` 회 이상 되풀이된 수치는 첫 등장 형광펜·이후 `<u>` 입니다(모두 인라인 style).
   같은 핵심 수치가 썸네일 배지(`images.thumbnail(badge=)`)에도 들어갑니다.
4. 점선 상자(📷 이미지) 자리에 사진을 넣고 상자는 지웁니다
5. 맨 아래 해시태그는 그대로 둡니다 — **네이버가 본문 해시태그를 태그로 자동 등록합니다**

안내문과 상단 바는 복사 범위에 들어가지 않으니 그대로 두셔도 됩니다.

글의 문체도 네이버에 맞춰 씁니다.

- 검색으로 들어온 사람은 스크롤을 안 하므로 **첫 3줄에 결론**
- 휴대폰으로 보므로 **한 문단 2~3문장, 120자 이내**
- 제목 앞쪽에 핵심 키워드 배치
- 소제목 4~6개, 이미지 자리 3곳
- 태그 20개 (넓은 키워드 + 좁은 키워드 섞어서)

이 규칙은 `config/settings.yaml` 의 `blog.naver` 에서 조절합니다.

```yaml
blog:
  platform: naver          # markdown 으로 바꾸면 티스토리·워드프레스용
  naver:
    tag_count: 20          # 네이버 태그 상한은 30개
    image_slots: 3
    paragraph_max_chars: 120
```

---

## 내 채널에 맞게 고치기

품질을 좌우하는 건 `config/settings.yaml` 의 `video` 섹션입니다. 여기부터 손보세요.

```yaml
video:
  channel_name: "방구석 배경지식"
  audience: "뉴스는 보지만 정치 용어와 국회 절차가 낯선 20~50대 유권자"
  tone: "차분하고 담담한 사실 전달. 어느 정당·인물의 편도 들지 않는다. 양쪽 주장은 나란히 싣고, 사실과 주장을 구분해 쓴다."
  persona: "정치 뉴스를 풀어 주는 진행자. 누가 무엇을 했는지, 절차상 다음이 무엇인지 설명하되 옳고 그름을 판정하지 않는다."
  shorts_seconds: 60         # 쇼츠 목표 길이
  longform_minutes: 8        # 롱폼 목표 길이
  speaking_rate_cpm: 330     # 본인 낭독 속도(분당 글자 수)
  banned_phrases:            # 대본에 절대 안 쓸 표현
    - "무조건 오른다"
    - "지금 안 사면 늦는다"
  cta: "도움이 되셨다면 구독과 알림 설정 부탁드립니다."
```

`audience` 와 `tone` 은 대본 문체에 그대로 반영됩니다. 구체적으로 쓸수록 결과가 좋아집니다.

다루는 주제를 바꾸려면 `config/sources.yaml` 의 `keywords` 를 조절합니다.
`weight` 가 높은 카테고리가 이슈 선정에서 우선합니다. 예를 들어 재건축 채널이라면
`공급·정비사업` 의 weight 를 3.5 정도로 올리세요.

---

## 뉴스 소스 관리

기본 소스는 **구글뉴스 키워드 피드 7개**(안정적)와 **언론사 정치 섹션 피드 11개**(통신사·신문·방송·경제지를
성향 고르게)입니다. 매체 가중치를 성향으로 기울이지 않습니다 — `sources.yaml` 머리 주석을 보세요.
언론사 RSS 주소는 개편 때 자주 바뀌므로 가끔 점검해 주세요.

- 설치 없이: **Actions** 탭 → **피드 점검** → **Run workflow**
- 내 PC에서: `python -m rebrief doctor`

워크플로는 4개입니다.

| 워크플로 | 언제 도는가 |
| --- | --- |
| 정치 데일리 브리핑 | 매일 21:45 UTC(= 06:45 KST) + 07:25 안전망 + 수동 |
| 사이트 배포 | `output/` 이 바뀌면 자동 + 수동 |
| 피드 점검 | 수동 |
| 테스트 | 푸시할 때마다 |

```
  ✅  gnews_politics        100건  구글뉴스·정치
  ❌  joongang_politics     항목 없음   ...

계속 실패하는 피드는 config/sources.yaml 에서 다음처럼 꺼두세요:
  - id: joongang_politics  →  enabled: false
```

언론사 직접 피드는 **본문까지 긁어오기 때문에** 요약 품질이 더 좋습니다.
구글뉴스 링크는 리다이렉트 페이지라 본문 수집을 건너뜁니다(제목·요약만 사용).
가능하면 언론사 피드를 살려 두세요.

---

## 비용

하루 한 번 실행 기준, Claude 호출은 3번입니다(사실 정리 → 블로그 → 대본).
2번째 호출은 첫 호출의 입력을 캐시로 재사용합니다.

**영상 대본은 `llm.script_model` 로 따로 지정합니다.** 출력 토큰이 가장 많은 호출이라
값싼 모델로 돌리면 하루 비용이 눈에 띄게 줄고, 브리핑·블로그 품질은 그대로입니다.
캐시는 모델마다 따로 잡히므로 대본 모델이 다르면 그 호출에는 캐시를 쓰지 않습니다.
호출별 비용은 실행 결과와 `state/costs.json` 의 `by_call` 에 남습니다.

| 모델 | 하루 | 한 달(30일) |
| --- | --- | --- |
| `claude-opus-5` (기본) | 약 $0.4 | 약 $12 |
| `claude-sonnet-5` | 약 $0.17 | 약 $5 |

실행할 때마다 실제 사용량과 추정 비용이 출력됩니다.

```
💰 claude-opus-5 · 3회 호출 · 입력 40,000 (캐시읽기 20,000) / 출력 9,000 토큰 · 약 $0.435
```

더 저렴하게 쓰려면 `config/settings.yaml` 에서 바꿉니다.

```yaml
llm:
  model: claude-sonnet-5
  effort: medium      # low | medium | high | xhigh | max
```

---


### 품질 장부와 쉬어 가는 날 (2026-09-08)

**`state/quality.json` (`store.QualityLog`).** 날마다 점검표 요약(ok/warn/fail), 숫자 검산
(확인/미확인/대조 불가), 쓴 모델, 본문 길이, 비용을 남깁니다. 전부 이미 만들던 값이라 담기만
합니다. `compare(days)` 가 최근 N일과 그 앞 N일의 평균을 견주고 대시보드가 표로 냅니다.
모델·프롬프트를 바꾸기 **전에** 며칠 쌓여 있어야 뒤에 비교가 됩니다.

**`rank.quiet_day`.** 판정 기준은 하나 — 여러 매체가 함께 다룬 이야기가 있는가. 없으면
LLM 호출을 건너뛰고 `quiet.json` 을 남깁니다. `store.failure_streak` 이 그 날을 실패로 세지
않도록 `quiet.json` 을 건너뜁니다. 설정은 `run.quiet_day` (기본 켜짐, `min_top_size: 3`).
판정이 애매하면 만드는 쪽으로 기웁니다.

**`render.asof_note` / `site._age_days`.** 글의 시점을 밝힙니다. 사이트 배지는 30일부터.
`site_page.html.j2` 는 결산·목록 등 여러 곳이 쓰므로 `age_days|default(0, true)` 로 받습니다.

**`render.policy_region_links` / `render.copy_stats_images`.** 실거래 통계와 짝을 이루던 자리라 이 저장소에서는
빈 값을 돌려주고 템플릿이 조용히 넘어갑니다.

### 모델이 쓰는 글 줄이기 (2026-09-07)

9월 7일 청구서 $0.6441 의 내역은 **출력 72.2% · 캐시 쓰기 20.2% · 입력 7.6%** 였습니다.
줄일 곳은 입력이 아니라 출력이라, 여섯 가지를 고쳤습니다.

| 고친 것 | 어디 | 근거 |
| --- | --- | --- |
| 브리핑을 sonnet 으로 | `llm.brief_model` | 9/6 자료로 실호출: 출력 8,706토큰 → opus $0.27 vs sonnet $0.106 |
| 접두사 캐싱 제거 | `llm._call` | 캐시 쓰기 20,814 · 읽기 0. 스키마가 접두사에 들어가 공유 불가 |
| 근거 기사를 번호로 | `prompts.article_ids` | 브리핑 출력의 20%(1,706자)가 주소였음 |
| 설명란·고정 댓글을 프로그램이 | `render.youtube_description` | 챕터·출처는 이미 가진 값 |
| 후보 5개 → 3개 | `models.LongformScript` | 어차피 하나만 고름 |
| B롤 구간마다 2개까지 | `models.LongformSection` | 여섯 구간이면 최대 24개였음 |

**캐싱을 다시 켜려면** 두 호출이 system 과 **구조화 출력 스키마까지** 같아야 합니다. 실측으로
확인했습니다 — 같은 스키마면 8,649토큰이 읽히고, 스키마만 바꾸면 읽기 0에 통째로 다시 씁니다.

**번호 규칙은 두 곳이 같아야 합니다.** `prompts.format_clusters` 가 붙이는 `(이슈-기사)` 번호와
`prompts.article_ids` 가 만드는 표가 어긋나면 근거가 통째로 빕니다. 한쪽을 고치면 다른 쪽도
고치세요. 모델이 번호를 하나도 안 주면 `_fill_source_urls` 는 기존 주소를 지우지 않습니다.

## 명령어

| 명령 | 설명 |
| --- | --- |
| `python -m rebrief run` | 전체 실행 (수집 → 요약 → 대본) |
| `python -m rebrief run --no-llm` | 수집·정리까지만. API 호출 없음 |
| `python -m rebrief run --limit 20` | 기사 20건만. 테스트용 |
| `python -m rebrief collect` | 수집만 하고 원본 저장 |
| `python -m rebrief render --date 2026-09-06` | 저장된 원본으로 산출물만 재생성 |
| `python -m rebrief weekly` | 지난 7일치를 묶은 주간 결산 (`--end 2026-09-06`, `--no-llm`) |
| `python -m rebrief monthly` | 지난달 한 달치를 묶은 월간 결산 (`--month 2026-08`, `--no-llm`) |
| `python -m rebrief profile "이름"` | 정치인 한 사람의 자료 정리표. `--pack` 자료묶음, `--llm` AI 정리 글(유료), `--birth 1964` |
| `python -m rebrief answer --date … --file 답.md` | 채팅(claude.ai)에서 받은 답(JSON 세 덩이)으로 글·카드·대본 만들기. 모델 호출 없음 (「답 받기」 워크플로가 이슈 본문으로 함) |
| `python -m rebrief civics --prefetch` | 맥에서 국회·여론조사 자료를 미리 받아 `state/civics/<날짜>.json` 으로 (깨우미가 자동으로 함) |
| `python -m rebrief civics --refresh --date …` | 아침에 못 받은 국회 자료를 다시 받아 `civics.md`·`json` 보충 (낮 워크플로가 함) |

산출물 그림은 PNG 로 바뀌면 SVG 를 지웁니다(`images.keep_svg: false`). 통계 그림은 사이트에서
참고로 보는 자료라 `images.png_scale_stats: 1` 로 내렸고, 카드뉴스도 1080×1080 이 최종 크기라
`images.png_scale_cards: 1` 입니다(2배로 두면 하루 6.4MB). 대시보드의 '산출물 용량' 칸이 하루 평균과
1년 뒤 크기를 보여 줍니다 — 자동으로 지우지는 않습니다.
| `python -m rebrief notify` | 오늘 결과를 텔레그램으로 (`--failed` 는 실패 알림) |
| `python -m rebrief titles show` / `titles log --date … --kind … --pick N --views X` | 제목 후보 보기 / 고른 것 기록 |
| `python -m rebrief publish --date … --url … [--views N]` | 발행한 글 주소 기록 (사이트에 '발행함' 표시) |
| `python -m rebrief site` | 휴대폰용 사이트 생성 (`site/`) |
| `python -m rebrief policy` | 정부 발표 원문 찾기·요약 (`--days 7`, `--no-llm`) |
| `python -m rebrief stats` | (부동산 유산, 꺼 둠) 실거래가 직접 집계. 이 저장소에서는 쓰지 않습니다 |
| `python -m rebrief doctor` | 피드 상태 + 정책브리핑 + 국회·여론조사 출처 점검 |

공통 옵션: `-v` (상세 로그), `--config <폴더>` (설정 경로 지정)

글이나 대본이 마음에 안 들면 `settings.yaml` 을 고치고 `render` 로 다시 만드세요.
기사를 다시 긁지 않으니 빠르고, 그날의 수집 결과는 그대로 유지됩니다.

---

## 문제 해결

**`python` 을 쳤는데 아무 반응이 없어요**

- **macOS**: `python` 이 아니라 `python3` 입니다. `python` 은 원래 없습니다.
  `python3 --version` 을 쳐 보세요. 이것도 안 되면 파이썬 설치가 필요합니다 → `bash setup.sh`
- **Windows**: 마이크로소프트 스토어가 열리면 아직 설치가 안 된 것입니다.
  [python.org](https://www.python.org/downloads/) 에서 받으면서 **"Add python.exe to PATH"** 를 꼭 체크하세요.
  또는 `setup.bat` 을 더블클릭하면 안내가 나옵니다.
- 애초에 설치가 번거로우면 **방법 A(GitHub Actions)** 를 쓰세요. 로컬 파이썬이 전혀 필요 없습니다.

**`ModuleNotFoundError: No module named 'feedparser'`**

가상환경을 켜지 않았거나 패키지 설치가 안 된 상태입니다.

```bash
source .venv/bin/activate      # Windows: .venv\Scripts\activate
pip install -r requirements.txt
```

**피드가 전부 실패해요**

개별 피드 문제가 아니라 네트워크·방화벽 문제일 가능성이 큽니다.
이럴 때는 `sources.yaml` 을 건드리지 마시고, 다른 네트워크에서 다시 시도하거나
**방법 A(GitHub Actions)** 로 돌려 보세요.

**수집은 되는데 요약이 안 나와요**

`ANTHROPIC_API_KEY` 가 없거나 잘못된 상태입니다. `prompt-pack.md` 가 대신 생성됐는지 확인하세요.
그 파일을 챗봇에 붙여넣으면 같은 결과를 얻을 수 있습니다.

---

## 구조

```
rebrief/
  collect.py     RSS 수집, 제목 정규화, 중복 제거, 본문 추출
  cluster.py     같은 사건 묶기 (글자 2-gram Dice 유사도)
  rank.py        이슈 점수 (보도량·키워드·최신성·매체·속보)
  llm.py         Claude 호출. 구조화 출력으로 Pydantic 모델 직접 수신
  prompts.py     프롬프트 3종 + 키 없을 때 쓰는 프롬프트 팩
  render.py      마크다운·네이버 HTML·SRT·JSON 생성
  images.py      수치를 인포그래픽 SVG 로. 크롬이 있으면 PNG 도
  weekly.py      일주일치를 묶은 주간 결산
  monthly.py     한 달치를 묶은 월간 결산
  linkcheck.py   출처 링크 생존 확인
  checklist.py   발행 전 점검표 (금지 표현·분량·자리 검사)
  verify.py      브리핑 수치를 기사 원문과 대조
  notify.py      텔레그램 알림
  store.py       수집 이력·비용 장부·수치 시계열·제목/발행 기록 (state/)
  keynumbers.py  오늘의 핵심 수치 고르기 + 수치 토큰 규칙
  policy.py      정책브리핑 보도자료 찾기·첨부 내려받기·3줄 요약·시행일 뽑기
  civics.py      국회(열린국회정보)·여론조사(선거여론조사심의위) 직접 세기 + 이슈 속 법안 추적
  stats.py       (꺼 둠) 실거래가·부동산원 통계 — estate-news 유산
  sanitize.py    모델 출력 HTML 에서 실행 가능한 조각 제거
  site.py        output/ 을 휴대폰용 정적 사이트로 변환
  pipeline.py    전체 조립
  cli.py         명령줄
  templates/     산출물 서식 (여기만 고쳐도 결과 형식이 바뀝니다)
config/
  sources.yaml   피드 목록 + 키워드 사전
  settings.yaml  파이프라인·영상·블로그 설정
```

같은 기사를 이틀 연속 다루지 않도록 `state/seen.json` 에 이력을 남깁니다
(기본 3일, `run.skip_recent_days` 로 조절).

---

## 알아 둘 것

- **수치는 반드시 원문과 대조하세요.** 프롬프트에서 "자료에 있는 내용만 쓰라"고 강하게 묶어 두었지만,
  숫자를 그대로 발행하기 전 확인하는 습관이 안전합니다. `production-notes.md` 4절에
  모델이 스스로 표시한 "확인 필요" 항목이 모여 있습니다.
- **RSS 는 제목과 요약만 주는 경우가 많습니다.** 본문 수집이 막힌 매체는 요약 근거가 얕아집니다.
  피드 점검으로 언론사 직접 피드를 최대한 살려 두는 게 품질에 가장 크게 기여합니다.
- **이슈 묶기는 완벽하지 않습니다.** 같은 인물·기관을 다룬 다른 기사가 한 이슈로 묶일 수 있습니다.
  자주 그러면 `cluster.similarity_threshold` 를 0.45 정도로 올리세요 (근거는 `cluster.py` 주석 참고).
- **어느 편도 들지 않습니다.** 프롬프트·매체 가중치·점검표 세 겹으로 막고, 여론조사를 인용한 글에는
  '자세한 사항은 중앙선거여론조사심의위원회 홈페이지 참조' 가 자동으로 붙습니다.

---

## 테스트

```bash
pip install -r requirements-dev.txt
python -m pytest tests/ -q
```

네트워크 없이 픽스처로 전 구간을 검증합니다. 템플릿은 `StrictUndefined` 라
변수 이름을 하나만 틀려도 테스트에서 바로 잡힙니다.
