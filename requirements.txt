# 핵심
feedparser>=6.0.11        # RSS/Atom 파싱
requests>=2.32.0          # HTTP 수집
PyYAML>=6.0.1             # 설정 파일
Jinja2>=3.1.4             # 산출물 템플릿
Markdown>=3.6             # 네이버 붙여넣기용 HTML 변환
anthropic>=0.69.0         # Claude API (요약/대본 생성). pydantic 포함

# 본문 추출(선택). 없으면 자동으로 건너뜀.
beautifulsoup4>=4.12.3
lxml>=5.2.0
pypdf>=6.1.3          # 정부 보도자료 첨부 PDF 에서 본문 뽑기
fonttools>=4.53.0     # 카드뉴스에 프리텐다드를 쓴 글자만 잘라 심기 (없으면 시스템 글꼴)
