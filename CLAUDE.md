# 이 저장소를 처음 여는 Claude에게

작업 전에 이 파일을 먼저 읽으세요. **사용자에게 이미 아는 것을 다시 묻지 않기 위한 메모입니다.**
새로 확정된 사실이 생기면 여기에 추가하세요.

---

## 사용자에 대해

- 유튜브 콘텐츠 제작자이자, 영상 제작을 의뢰받아 납품하는 프리랜서입니다.
- **개발자가 아닙니다.** 전문 용어를 쓰지 말고, 클릭 경로까지 짚어서 설명하세요.
  터미널 작업을 전제하지 말고 브라우저(깃허브 화면)로 끝나는 방법을 우선 제시하세요.
- 한국어로 소통합니다.

## 이 저장소의 출신

**`estate-news`(부동산 브리핑)를 2026-09-08 에 통째로 복사해 정치 뉴스용으로 고친 것입니다.**
파이프라인·카드뉴스·사이트·배포·점검표는 그대로이고, 부동산 전용이던 것만 바꿨습니다.

| 바꾼 것 | 어떻게 |
| --- | --- |
| 피드·키워드 (`config/sources.yaml`) | 정치 피드 18개(통신사·신문·방송·경제지를 성향 고르게), 키워드 7묶음 |
| 프롬프트 (`rebrief/prompts.py`) | 애널리스트 절대 규칙에 **편들기 금지·양쪽 병기·주장/사실 구분·의혹 표현·여론조사 개요** 추가 |
| 금지 표현 (`video.banned_phrases`) | 진영이 상대를 부르는 말 14개. 점검표가 실제로 검사 |
| 용어 사전 (`rebrief/glossary.py`) | 국회 절차·선거·사법 용어 50개. **풀이에 평가를 넣지 않음** |
| 사진 검색어 (`rebrief/photos.py`) | 국회의사당·정부청사·투표소 같은 **건물·장소**. 사람 얼굴은 피함 |
| 정부 발표 (`rebrief/policy.py`) | STRONG 낱말을 국무회의·개각·재의요구 등으로 |
| 점검표 (`rebrief/checklist.py`) | **여론조사 인용 표기 검사**(`poll_citations`) 추가 |
| 통계 (`stats.enabled: false`) | 실거래·부동산원 모듈은 **꺼 둠**. 대신 `rebrief/civics.py`(국회·여론조사)가 '직접 센 숫자' 자리를 맡음 |

**꺼 둔 부동산 유산** — `rebrief/stats.py`(905줄)·`keynumbers`의 일부·`regions.py`·`images.py` 의
서울 자치구 도식·`templates/stats.md.j2`·weekly/monthly 의 실거래 절은 `stats.enabled: false` 라
돌지 않습니다. 정치용 '직접 센 숫자' 를 만들 때 이 자리를 갈아 끼우면 됩니다 (아래 '다음에 할 일').
estate-news 의 통계 관련 교훈(실거래 신고 기한, 부동산원 CLS_ID 등)은 이 파일에서 뺐습니다 —
필요하면 `../estate-news/CLAUDE.md` 를 보세요.

## 확정된 사실 (묻지 마세요)

| 항목 | 값 |
| --- | --- |
| 폴더 | `Active/politics-news` (2026-09-08 생성) |
| 저장소 | `rokieyez/politics-news` — 2026-09-08 생성(공개). Pages 는 워크플로 배포(`build_type: workflow`) |
| 기본 브랜치 | `main` |
| **도메인** | **`rokiz.net` 은 사용자가 직접 구매한 개인 도메인입니다.** |
| 사이트 주소 | `https://www.rokiz.net/politics-news/` (사용자 도메인 아래 프로젝트 경로. CNAME 설정 불필요) |
| 범위 | **국내 정치** — 국회·정당·대통령실·선거·여론조사·사법(정치 관련). 외교·안보는 낮은 가중치 |
| 채널명 | **「방구석 배경지식」** (2026-09-08 사용자 확정). 로고는 `assets/logo.png`(150×150, 책 아이콘) — 사용자가 「로고 맞음」으로 확정 |
| 블로그 | 네이버 블로그 **`blog.naver.com/keyboardpolitics`** (정치용으로 따로 만듦). `blog.naver.write_url` 에 글쓰기 주소 |
| 저장소 공개 여부 | 공개(public) |
| 인증키 | `.env` 는 estate-news 것을 복사함(내용은 보지 않음). 깃허브 Secrets 는 2026-09-08 사용자가 넣었고 첫 실행이 $0.3558 로 성공(`ASSEMBLY_API_KEY` 도 동작 — 발의 147·본회의 18·여론조사 9건을 셌음) |

`rokiz.net` 은 저장소 Settings → Pages 의 커스텀 도메인으로 설정합니다.
**리포지터리에 CNAME 파일을 두지 마세요.** `build_site()` 가 `site/` 를 매번
삭제 후 재생성하므로 파일로 두면 배포 때마다 사라집니다.

## 정치 뉴스라서 다른 것 — 반드시 지킬 것

**어느 편도 들지 않습니다. 이 프로젝트가 가장 피해야 할 일입니다.**
사실을 하나도 틀리지 않아도 한쪽 주장만 옮기면 브리핑이 그쪽 편이 됩니다. 세 겹으로 막습니다 —
① 프롬프트(양쪽 병기·주장과 사실 구분·평가어 금지) ② 매체 가중치를 성향으로 기울이지 않음
(`sources.yaml` 머리 주석) ③ 점검표의 금지 표현 검사. 프롬프트만으로는 보장이 안 되므로
산출물을 실제로 검사합니다. 균형은 언급 수와 발언 인용 수를 따로 잽니다(아래 '균형 검사').

**여론조사를 인용하면 조사 개요를 같이 씁니다 (공직선거법 제108조).**
조사기관·의뢰자·조사 기간·표본 크기·응답률·오차범위를 밝히고 "자세한 사항은
중앙선거여론조사심의위원회 홈페이지 참조" 를 답니다. 마지막 문장은 `blog.disclaimer` 에 넣어
프로그램이 늘 붙이고, 나머지는 `checklist.poll_citations` 가 빠진 항목을 ⚠️ 로 올립니다.
오차범위 안의 차이를 "앞선다" 로 쓰지 않습니다(프롬프트 규칙 9).
**선거일 전 6일부터 투표 마감까지는 새 여론조사 결과를 공표할 수 없습니다** — `elections:` 달력으로
검사합니다(아래 '선거 기간 검사'). **글이 스스로 "조사 개요가 자료에 없다" 고 밝힌 문장이 있으면 경고하지
않습니다** (`checklist.poll_self_disclosed`, 2026-09-08). 연설 속 숫자를 옮기며 출처 없음을 적은 글은 인용이
아니라 발언 보도인데, 정직하게 쓴 글이 ⚠️ 를 받으면 경고가 무뎌집니다(실제 9/8 글이 그랬음).

**의혹·수사 단계는 확정처럼 쓰지 않습니다 (명예훼손).**
"혐의를 받고 있다", "의혹이 제기됐다" 로 쓰고 유죄 판결 전에 범죄자로 부르지 않습니다.
당사자 해명이 자료에 있으면 반드시 같이 싣고, 없으면 caution 에 적게 했습니다(프롬프트 규칙 7·8).

**사람 얼굴이 나오는 사진·영상은 쓰지 않습니다.**
검색어는 **전부 서울의 건물·장소**에 묶습니다 (2026-09-08 사용자 지시, `photos.QUERY_MAP`). Pexels 실측:
'courthouse'·'supreme court' 는 seoul 을 붙여도 미국 대법원이 오고, 'election voting' 은 투표하는 사람,
'gyeongbokgung' 은 한복 관광객이 옵니다. 그래서 사법·선거·집회도 'seoul gwanghwamun'(밤의 문루)·
'seoul city hall' 같은 건물 낱말로 돌리고, 설명글(alt)에 사람이 보이면 `_shows_people` 이 한 번 더 뺍니다.
스톡 사진 속 인물이 특정 정치인으로 오해될 수 있고, 무관한 사람이 정치 기사 옆에 붙는 것도
문제입니다. 사진 검색어는 건물·장소로만 잡고(`photos.QUERY_MAP`), 대본의 화면 지시도
"정치인 얼굴 사진·영상은 지시하지 마세요" 로 막았습니다.

**`exclude_terms` 에 '인사' 를 넣지 마세요.**
estate-news 는 인사 발령 알림을 버리려고 낱말 '인사' 를 막았는데, 정치에서는 '인사청문회'
기사가 통째로 빠집니다. `exclude_patterns` 의 `^\[인사\]` 로 말머리만 거릅니다.

**국회·여론조사는 직접 셉니다 (`rebrief/civics.py`, 2026-09-08, 사용자 지시 ①).**
실거래가 자리를 갈아 끼운 정치용 '직접 센 숫자' 입니다. 두 곳을 읽습니다.
* **열린국회정보(open.assembly.go.kr)** — 국회의원 발의법률안 `nzmimeepazxkubdpn` · 본회의 처리안건
  `nwbpacrgavhjryiph`. **인증키 없이도 응답하지만 견본 모드라 한 번에 5건뿐**입니다 (2026-09-08 실측:
  `pSize=100` 을 줘도 5건, `KEY=sample` 같은 가짜 키는 ERROR-290 으로 거부 → 키가 없으면 `KEY` 를
  아예 빼고 부릅니다). 그래서 키가 없으면 **건수를 세지 않고**(`count=None`) 최근 5건만 보여 줍니다 —
  5건을 '이번 주 5건' 으로 적으면 거짓말입니다. `ASSEMBLY_API_KEY`(회원가입 → 마이페이지 → 인증키
  신청, 무료)를 넣으면 쪽을 넘겨 셉니다. 발의안 목록은 최신순이라 날짜가 앞으로 넘어가면 멈추고,
  본회의 처리안건은 의안번호 순이라 다 넘겨야 합니다(1,700여 건 = 18쪽).
* **선거여론조사심의위(nesdc.go.kr) 등록부** — 인증키 없음. 목록 화면(`B0000005/list.do`)은
  `<a class="row tr">` 안에 `<span class="col">` 여덟 칸(등록번호·기관·의뢰자·방법·추출틀·명칭·등록일·
  시도)이고, 상세 화면의 th/td 짝에서 조사일시·조사완료 사례수·전체 응답률·표본오차·공표 매체를
  읽습니다. `Poll.summary()` 가 선거법 108조가 요구하는 항목을 한 줄로 만듭니다 —
  「여론조사공정(주) · 펜앤마이크 의뢰 · 2026-09-06~07 · 1,015명 · 무선 ARS · 응답률 2.1% · 95% 신뢰수준에
  ±3.1%p」. 2026-09-08 실측으로 최근 7일 등록 9건을 읽었습니다.
값은 `civics.md`(사이트 '국회·여론조사 집계')·블로그의 '직접 센 숫자' 상자·영상 대본 user 프롬프트에
프로그램이 그대로 넣습니다. 한 곳이 죽어도 다른 곳 값은 남고, 둘 다 죽으면 `{}` 라 글은 그대로
나갑니다. 시험은 `tests/test_civics.py`, 망은 `conftest` 가 `civics.enabled=False` 로 끕니다.
**Jinja 에서 `plenary.items` 는 dict 메서드에 잡힙니다** — 템플릿은 `plenary["items"]` 로 씁니다
(실제로 걸렸음).

**선거 기간 검사 (`checklist.election_blackout`, 사용자 지시 ②).** `elections:` 달력(설정)의
선거일 전 6일부터 선거일까지, 글에 여론조사 수치가 있으면 ❌, 없으면 ⚠️ 안내. 지금 달력에는
제23대 총선(2028-04-12, 임기만료일 2028-05-29 전 50일 이후 첫 수요일)만 있습니다. **보궐선거가
잡히면 사용자가 한 줄 넣어야 합니다** — 프로그램이 선거 일정을 알아서 찾지는 않습니다.

**균형 검사 (`checklist.side_balance`, 사용자 지시 ③).** `balance.sides` 의 별칭으로 두 진영 언급을
세어 합이 `min_total`(6) 이상이고 한쪽이 `max_share`(75%)를 넘으면 ⚠️. 긴 별칭부터 세고 지워
'더불어민주당' 안의 '민주당' 을 두 번 세지 않습니다. **여당·야당 별칭은 정권에 따라 바뀝니다** —
2026-09 기준 여당은 더불어민주당입니다. **발언 인용도 따로 셉니다** (`quote_balance`, 2026-09-08) — 발언
서술어(말했다·밝혔다·주장했다…)가 든 문장에서 먼저 나온 진영을 화자로 봅니다. 언급은 고른데 옮긴 말이
한쪽뿐이면(합 `min_quotes` 4 이상, 한쪽 75% 초과) ⚠️.

**오늘 이슈에 나온 법안은 지금 어디에 (`civics.track_issue_bills`, 2026-09-08).** 브리핑이 나온 뒤 이슈
제목·한 줄·사실 항목에서 '…법' 을 뽑아(`law_names` — 후속입법·방법 같은 것은 접미사 목록으로 거름) 열린국회정보를
`BILL_NAME` 부분 일치로 찾고, 가장 최근 계류 안건의 단계(`bill_stage`)를 `civics.md`·블로그 상자에 한 줄로
넣습니다. 견본 모드에서도 건수(`list_total_count`)는 맞아 키 없이도 돕니다(계류 수만 행이 다 왔을 때 셈).
실측 9/8: 공소청법 3건(계류 2, 소관위 상정) · 형사소송법 161건(소관위 회부). 모델은 부르지 않습니다.

**국회 자료는 맥이 먼저 받고, 러너는 이어받습니다 (2026-09-09, 사용자 선택 ①②).** 러너(해외)에서 열린국회정보가
흔들려 첫날부터 발의·본회의·추적을 못 받았기 때문입니다. 깨우미(`scripts/wake-daily-brief.sh`)가 워크플로를
부르기 전에 `python -m rebrief civics --prefetch` 로 `state/civics/<날짜>.json` 을 만들고, 작업 폴더가 아니라
`~/Library/Caches/rokiz/politics-news-wake` 사본에서 커밋·푸시합니다(작업 폴더의 편집 중인 것을 건드리지 않으려고).
러너 쪽 `pipeline._collect_civics` 는 그 파일이 견본이 아니면 통째로 쓰고, 견본이면 직접 받되 못 받은 항목만
채웁니다(`civics.merge_prefetch`). 이슈는 아직 없으니 기사 제목의 법안 이름을 미리 찾아 `tracked_pool` 에 두고,
`track_issue_bills` 가 풀에서 먼저 꺼냅니다. 실측 9/9 맥: 7.4초, 견본 모드(맥 `.env` 에 키 없음), 여론조사 9건,
풀 4개(선거법·행정수도특별법·지방의회법·경찰법). 낮에는 「국회 자료 보충」 워크플로(03:00 UTC 예약)가
`civics --refresh` 로 못 받은 항목만 다시 받아 `civics.md`·`json` 을 채웁니다 — 블로그 본문은 이미 나간 뒤라
건드리지 않고, 카드도 사진 캐시가 러너에 없어 다시 만들지 않습니다. 시험은 `prefetch_path` 를 임시 폴더로
돌려야 합니다 — `Config.state_dir` 는 픽스처에서도 진짜 `state/` 입니다(9/9 에 시험이 진짜 파일을 덮어쓴 적 있음).

**본회의 표결 카드 (`images.pick_vote` / `_vote_card`, 2026-09-08).** civics 의 처리안건 가운데 **실행일과
전날** 표결만 골라(아침 브리핑은 전날 일을 다룸) 이슈 제목과 낱말이 겹치는 법안, 없으면 표가 가장 많은 법안
한 건을 찬성(금)·반대(상아)·기권(낮은색) 막대로 그립니다. 표지 다음, 숫자 카드 앞. 일주일 전 표결은 뉴스가
아니라 안 만듭니다 — `test_vote_card_appears_only_for_a_fresh_plenary_vote`.

**수집 상한 `run.max_articles: 500` (사용자 지시 ④).** 정치는 하루 900건이 넘어 300 이면 잘렸습니다.

**카드 색은 「의사당」 판입니다 (2026-09-08, 시안 셋 중 사용자가 고름, 지시 ⑤).**
짙은 초록(`#0f2f2a`·`#08201c`)·상아(`#e9f0ea`)·금(`#eac56a`, 상아 위에서는 짙은 금 `#7a5a12`).
시안은 관보(먹+금)·의사당(초록+금)·석재(차콜+연보라) 셋을 실제 카드로 그려 보냈습니다.
**빨강·파랑 계열은 바탕·신호색 어디에도 쓰지 않습니다** — 정당 상징색이라 카드가 그 당 것으로 읽힙니다.
`test_card_palette_stays_readable` 가 대비 4.5 와 함께 색상(hue)으로 빨강(345~15°)·파랑(200~250°)을
막습니다. 철학 문서는 `docs/카드뉴스-디자인-철학.md` 를 「의사당」 판으로 다시 썼습니다. 아래
물려받은 결정의 '카드 디자인은 「청사진」 판' 문단은 색 이름만 옛것이고 뼈대 규칙은 그대로입니다.

## 0원 방식 (2026-09-10, 사용자 선택 「0원방식」)

사용자가 「크레딧 사용량이 부담스럽다」고 해서, 하루 $0.5 짜리 API 호출 셋을 **claude.ai 구독 채팅 붙여넣기**로 바꿨습니다.
설계와 함정은 `docs/기술문서.md` '0원 방식' 절. 여기엔 다시 만들 때 틀리기 쉬운 것만.

- `llm.enabled: false` 가 스위치입니다. 아침 데일리는 수집·civics·묶음(`prompt-pack.md/.html`)·`pack.json` 까지만.
  되돌리려면 `true` — 키는 Secrets 에 그대로 있습니다(인물 조사 `--llm` 도 그 키를 씁니다).
- 답은 **이슈**로 받습니다(제목 「답 YYYY-MM-DD」, `answer.yml`). 토큰·설정이 필요 없고 휴대폰 깃허브에서 됩니다. rokiz.net/politics
  버튼 페이지(토큰 방식)로 옮기는 건 나중 아이디어.
- 산출 단계는 `_generate_with_llm` 과 `pipeline.answer` 가 **같은 세 함수**를 씁니다. 산출물을 고칠 땐 그 세 함수를 고치세요.
  `answer` 경로에는 generator 가 없어(정책 요약은 추출식, 금지 표현 자동 고침은 건너뜀) 그 둘만 API 경로와 다릅니다.
- 묶음의 기사 발췌는 500자(`PACK_EXCERPT_CHARS`)입니다 — 공개 저장소에 올라가서 API 프롬프트(1,200자)보다 짧습니다.
  브리핑 품질이 떨어지면 여기부터 의심. `pack.json` 은 본문 없이 숫자 토막만 — 검산은 그걸로 돕니다.
- 실측 9/10 저녁(기사 500·이슈 5): 묶음 35KB(약 2만 자), `pack.json` 35KB. 오늘 산출물 크기로 보면 답은 25~30KB 라
  이슈 본문 한도(65,536자) 안입니다. **실제 claude.ai 왕복은 아직 안 해 봤습니다** — 첫 아침에 답 형식이 어긋나면
  `answer.parse_answer` 의 관용 규칙(표식 없음·한 객체)을 넓히거나 묶음의 형식 지시를 손보세요.
- 시험: `tests/test_pipeline.py` 의 '0원 방식' 절. `ContentGenerator` 를 터지는 스텁으로 두고 `answer()` 가 모델을 안 부르는지 봅니다.

## 인물 조사 (`profile`, 2026-09-09 추가)

사용자 요청 「특정 정치인이 어떤 사람인지·어떤 발자취를 걸어왔는지 정리하는 영상의 자료 찾기를 자동화」로 만든
데일리와 **별개의** 명령입니다. 같은 날 사용자가 「가격이 부담된다」 → **기본은 모델 없이 정리표만(0원)**, 자료묶음(`--pack`,
claude.ai 에 붙여 넣기)과 AI 정리(`--llm`, 유료)는 선택으로 확정. 파일 이름 `이름(생년,정당)_날짜.md` 는 사용자 지정 형식.
버튼은 **rokiz.net/politics** (집 저장소 `rokieyez.github.io/politics/index.html`, 사용자 지정 주소). 자세한 것은
`docs/기술문서.md` '인물 조사' 절. 다시 만들거나 고칠 때 알아야 할 것:

- 출처 넷(열린국회정보 `ALLNAMEMBER`·발의법률안, 위키백과, 구글뉴스 검색 RSS)은 **전부 키 없이** 돕니다.
  이름으로 찾은 인적사항은 견본 제한 없이 통째로 옵니다(2026-09-09 실측). 사진(`NAAS_PIC`)은 받지만 쓰지 않습니다 — 얼굴 금지 원칙.
- 위키백과의 **역대 선거 결과·소속 정당 절은 표라서 평문 추출에 비어 있습니다.** 위키텍스트를 따로 받아 칸 단위로 풉니다
  (`wiki["tables"]`). 낙선 기록은 거기에만 있어 정리표의 '선거 이력' 이 여기서 나옵니다.
- 구글뉴스 조건에 '대표' 를 넣으면 '국가대표' 선수 기사가 섞입니다 → '당대표'. 정리표의 연도별 기사는 제목에 이름이 든
  기사가 셋 이상이면 그것만 남깁니다(본문만 맞은 잡음 제거).
- AI 정리의 논란은 의혹·해명·현재 상태 세 칸. 해명이 없으면 점검에 ⚠️. 위키백과에만 있는 사실은 `cautions` 로 분리.
- 산출물은 `output/profiles/이름-생년/` 에 커밋됩니다. 사이트(`site/`)에는 안 올리고 rokiz.net/politics 가 저장소를 직접 읽습니다.
- 첫 실측(김민석, 2026-09-09): 수집 18초, 자료묶음 약 55KB. AI 정리는 opus(effort high) 3분 15초, 입력 31,282·출력 13,571 토큰,
  **$0.496**. 결과는 연표 33줄·장 5개·논란 7건·확인할 것 10개. 모델이 동명이인 기사(피겨 선수)를 스스로 걸러 적었고,
  2008년 구속 사건의 재판 결과가 자료에 없다며 확정 서술을 피했음 — 프롬프트 규칙 7이 먹힘.
- **깃허브 서버에서는 열린국회정보(open.assembly.go.kr) 접속이 자주 끊깁니다 (2026-09-09 실측: 세 번 중 두 번 ConnectTimeout,
  세 번째는 정상).** 완전 차단이 아니라 느리거나 불안정한 것이라, `profile._get`·`civics._get` 이 연결 단계 실패만 **세 번까지,
  3초→12초 간격으로** 다시 부릅니다(2026-09-09 마무리 회차 2 에서 두 번→세 번으로. 효과는 다음 데일리에서 볼 것).
  **탐침 실측(2026-09-09 09:40 KST, 러너는 Microsoft 버지니아 Boydton):** 열린국회정보는 5회 모두 1초 안에 연결(HTTP 400 은
  KEY 없이 부른 탓), 같은 날 07:09 데일리는 세 번 다 20초 ConnectTimeout — 시간대에 따라 흔들립니다. korea.kr 과
  선관위(nesdc)는 서너 번 빠르게 부르면 **빈 응답(curl 52)** 으로 끊었습니다 — 해외 IP 속도 제한으로 보입니다. 그래서
  오늘 데일리의 '정책 원문 목록' 도 ConnectTimeout 이었습니다. 인물 조사 워크플로는 [A]·[B] 없이 위키백과 표로 기본 정보(생년·정당·당선)를
  채우고 안내문에 「닿지 못해」 라고 적습니다(`wiki_basics`). **데일리의 `civics.py` 도 같은 이유로 첫날부터 발의법률안·본회의를
  못 받고 있었습니다** (`output/*/civics.md` '받지 못한 것 — ConnectTimeout'). 여론조사(nesdc)는 됩니다. 근본 해결은 맥에서
  받아 저장소에 밀어 넣는 깨우미 확장이나 셀프호스트 러너 — 아직 안 함(아이디어 목록).
- 위키 문서 대조는 생년 ±1년을 봐줍니다 — 호적과 실제 생년이 다른 사람(이재명 1964/1963)이 있어 처음엔 못 찾았습니다.
  시기별 기사 검색의 시작 연도는 첫 당선 1년 전과 위키 정당·선거 표의 첫 연도 중 이른 쪽(이준석은 당선 전 당대표).
- **시험이 진짜 모델을 부른 사고(2026-09-09, 약 $0.5).** `cfg` 픽스처의 `load_config()` 가 `.env` 를 다시 읽어 autouse 가
  비운 `ANTHROPIC_API_KEY` 를 되살렸습니다. 이제 `cfg` 픽스처가 다시 비우고, `test_profile` 은 `ContentGenerator` 를 터지는
  스텁으로 막습니다. **모델을 부르는 경로의 시험은 반드시 스텁을 두세요.**

## 다음에 할 일

0. **0원 방식 첫 왕복 확인(2026-09-11 아침)** — 사이트 「붙여넣기 묶음」→ claude.ai → 「답 …」 이슈 → 2~3분 뒤 글·카드가
   생기는지. 안 되면 이슈 댓글의 이유와 `gh run list --workflow "답 받기"` 로그를 보고 `answer.parse_answer` 나 묶음 지시를 고칠 것.
   답의 품질(브리핑 수치·균형)도 API 시절 품질 장부(`state/quality.json`)와 견줄 것.
0. **네이버 붙여넣기 실물 확인** — 회색 배경 제거·`<hr>` 구분선(2026-09-08)은 맥 클립보드까지만 실측했습니다.
   사용자가 실제 네이버 에디터에 붙인 화면 한 장을 주면 여기 결과를 적으세요.
1. **데일리 국회 집계가 깃허브에서 자주 실패함** — 위 '인물 조사' 절의 접속 메모. 재시도(2026-09-09)로 나아지는지 며칠 `civics.md` 를
   볼 것. 여전히 빠지면 맥 깨우미가 `civics` 결과를 커밋하게 하거나 셀프호스트 러너.
1. ~~내일(2026-09-09) 아침 실행 확인~~ — **확인함(2026-09-09).** 깃허브 `schedule` 은 1시간 55분 늦게 옴(위 정정 참고).
   launchd 깨우미가 `workflow_dispatch` 로 두 번 깨웠습니다 — 07:08(06:45 예약이 맥이 깨어난 뒤 밀려서 돎, 8분 걸려
   브리핑 2026-09-09 커밋)과 07:25(방어막이 28초 만에 끝냄). 맥이 자고 있으면 첫 예약은 늦게 돌지만 두 번째가 받쳐 줍니다.
2. **보궐선거가 잡히면 `elections:` 달력에 한 줄** — 프로그램이 선거 일정을 찾지 않습니다.
3. **맥 `.env` 에 `ASSEMBLY_API_KEY` 넣기(사용자 몫)** — 없으면 맥의 미리 받기가 견본(5건)이라 러너가 국회에
   다시 묻고, 그게 흔들리면 결국 못 받습니다. 깃허브 Secrets 에 넣은 것과 같은 값을 `.env` 에 한 줄 추가하면
   다음 날 아침부터 맥이 완전한 자료를 밀어 넣습니다. 확인: `python -m rebrief civics --prefetch` 출력에
   「견본 모드」가 없어야 합니다.
4. ~~내일(2026-09-10) 아침 확인~~ — **확인함(2026-09-10).** 깨우미가 07:53 에 한 번만 돌았음(맥이 자고 있어 06:45·07:25 가
   깨어난 뒤 하나로 합쳐짐 — 정상 동작이고, 7시를 지키려면 맥이 그 시각에 깨어 있어야 함). 미리 받기는 견본 모드(키 없음)로
   3초 만에 밀어 넣었고, 러너는 오늘 직접 받는 데 성공(발의 216·본회의 18, ConnectTimeout 0). 카드 5장 전부 사진 있음.
   깨우미가 작업 폴더에 남긴 `state/civics/<날짜>.json` 이 나중에 `git pull` 을 막아서(추적하지 않는 파일 덮어쓰기)
   사본으로 옮긴 뒤 지우게 고침.

## estate-news 에서 물려받은 결정과 근거

아래는 estate-news 에서 실측으로 정한 것들이고 이 저장소에도 그대로 적용됩니다.
날짜·수치는 estate-news 에서 잰 값입니다.

**네이버 자동 발행은 하지 않습니다.**
네이버가 공개한 API 에 블로그 글쓰기 기능이 없습니다(검색 API 만 존재).
브라우저를 조작하는 자동 포스팅 프로그램은 네이버가 제재를 강화하는 대상이고
계정 정지 위험이 있어 배제했습니다. 글 생성까지 자동화하고, 마지막 붙여넣기만
사람이 합니다. `blog-naver.html` 의 복사 버튼이 그 30초를 담당합니다.

**클러스터 유사도 임계값 0.35 (Dice).**
실측: 같은 사건 0.35~0.69 / 다른 사건 0.00~0.40. 두 구간이 겹칩니다.
같은 사건이 두 꼭지로 갈리는 쪽이 더 나쁘다고 보고 재현율을 택했습니다.
근거는 `rebrief/cluster.py` 주석에 있습니다.

**보도량 상한 `rank.volume_cap: 8`.**
2026-09-06 실행에서 21개 매체가 받아쓴 건설사 보도자료가, 6개 매체가 다룬
종부세 분석을 점수로 이겼습니다. 상한 적용 후 순서가 바로잡혔습니다.

**도박·코인 SEO 스팸 차단.**
구글뉴스가 키워드 검색에 스팸을 섞어 보냅니다(수집 214건 중 26건, 12%).
`exclude_terms` 로 거릅니다. 용어 화이트리스트(`require_terms`)도
만들어 봤지만 멀쩡한 기사가 30건 넘게 걸려 기본값을 빈 목록으로 두었습니다.
한국어는 부분 문자열이 겹치므로('포커' ⊂ '포커스') 애매한 건 `exclude_patterns`
정규식으로 처리합니다.

**인포그래픽은 수치 생김새로 고릅니다 (주제 고정 아님).**
`rebrief/images.py` 가 `data.json` 의 datapoint 를 훑어 형태를 정합니다.
주제는 매일 바뀌므로 '종부세 지도' 같은 특정 주제에 고정하면 다음 날 아무것도
안 나옵니다. 서울 자치구 도식은 '자치구 N개' + '가·나·다 제외' 가 있는 날만
나오는 특수 케이스이고, 기본형은 수치 카드입니다.

**없는 숫자는 그리지 않습니다.**
2026-09-06 실행에서 '임대주택 공급 3년새 81% 감소' 는 감소율 하나뿐이고 연도별
물량이 보도에 없었습니다. 3년치 막대를 그리면 없는 값을 지어내는 셈이라
100 기준 지수 비교로만 그리고 각주에 밝혔습니다. 자치구 도식도 25칸에서
제외분을 뺀 수가 보도값과 어긋나면 그리지 않고 넘어갑니다.

**테스트는 브라우저를 띄우지 않습니다.**
PNG 변환에 크롬 헤드리스를 쓰는데, 테스트 픽스처가 실제 `config/` 를 읽는 탓에
처음엔 테스트마다 크롬이 떴습니다(전체 18초). `tests/test_pipeline.py` 의 `cfg`
픽스처에서 `images.png = False` 로 꺼서 1초로 돌아왔습니다. 이 줄을 지우지 마세요.

**그림은 블로그 글의 자리 라벨로 짝짓습니다.**
`BlogPost.image_slots[].datapoint_label` 을 모델이 채우고, 프로그램이 그 라벨로 수치를
찾아 `img-<자리번호>-<형태>.png` 로 만듭니다. 문자열 추측 매칭이 아니라 구조화 출력이라
확정적입니다. 라벨이 조금 달라도 Dice 0.6 이상이면 같은 수치로 봅니다.
네이버 HTML 의 미리보기 `<img>` 는 `class="nocopy"` 라 복사에서 빠집니다 — 상대 경로
이미지가 붙여넣기에 섞이면 에디터에 깨진 그림이 생기기 때문입니다.

**추이 그래프는 `state/datapoints.json` 이 사흘 이상 쌓여야 나옵니다.**
LLM 이 매일 라벨을 조금씩 다르게 쓰므로 정확 일치가 아니라 단위 동일 + Dice 0.6 으로
같은 지표를 묶습니다. 처음 며칠 추이가 안 나오는 건 버그가 아닙니다.

**쇼츠 초안 mp4 기능은 뺐습니다 (2026-09-07, 사용자 결정).**
자막 카드를 ffmpeg 로 이어 붙이던 `video.py` 를 삭제했습니다. 러너에 ffmpeg 가 없기도 했고, 사용자가 SRT 자막
파일(`script-shorts.srt`)로 직접 편집하는 쪽을 택했습니다. 다시 살리자고 하면 git 이력의 `video.py` 를 참고하세요.
Actions 아티팩트 업로드 단계도 같이 뺐습니다 — 산출물은 어차피 커밋되고, 아티팩트에는 기사 원문까지 통째로 올라갔습니다.

**주간 결산은 `output/weekly/<ISO주차>/`.**
일요일 08:20 KST(토 23:20 UTC) 에 돌고 LLM 1회. 지난 7일 중 `data.json` 이 3일 미만이면
만들지 않습니다. 비용은 `state/costs.json` 에 `kind: weekly` 로 구분해 쌓입니다.

**테스트 픽스처(`tests/conftest.py` 의 `cfg`)는 외부 프로세스·네트워크를 전부 끕니다.**
`images.png=False`, `collect.check_links=False`. 링크 점검·알림은
모듈 안의 `_head/_get/_post` 래퍼를 갈아끼워 검증합니다 — `requests` 전역을 패치하면
수집기 스텁과 충돌합니다(실제로 그랬음).

**깃허브 우분투 러너의 크롬은 "문서상 있음"이지 검증한 게 아닙니다 (2026-09-06 기준).**
데일리 워크플로에 'PNG 변환 도구 확인' 단계를 넣어 두었으니 첫 자동 실행 로그에서
`크롬 있음:` 줄을 확인하세요. 없으면 SVG 만 나오고 실행은 성공합니다.

**러너에는 한글 글꼴이 없습니다 — 워크플로가 `fonts-noto-cjk` 를 설치합니다 (2026-09-07).**
첫 자동 실행의 PNG 가 전부 네모(□)로 나왔습니다. 크롬은 있었지만 한글 글꼴이 없어서입니다. 데일리·주간
워크플로에 설치 단계를 넣었고 `images.FONT` 에 'Noto Sans CJK KR' 을 추가했습니다. 그날 그림은 맥에서 다시 변환해 커밋했습니다.

**티스토리 자동 발행은 불가능합니다 (2026-09-06 확인).**
티스토리 Open API 공식 문서에 "2024년 2월까지 순차 종료" 가 명시돼 있습니다. 글쓰기 API 가
없습니다. 워드프레스는 REST API 가 살아 있어 가능합니다. README 의 옛 문장은 고쳤습니다.

**숫자 검산은 LLM 없이 문자열 대조로만 합니다.**
`verify.py` — 값을 정규화해 근거 기사 제목·본문에 있는지 봅니다. '미확인' 은 틀렸다는 뜻이
아니라 "원문에서 그 표기를 못 찾았다" 입니다(단위·반올림이 다를 수 있음). 사람이 보게
`brief.md` 에 표로 올립니다.

**모델 강등은 한도·과부하·5xx·연결 오류에만.**
인증·거부(4xx)는 강등하지 않습니다. `Usage` 는 호출마다 쓴 모델 단가로 비용을 더합니다 —
강등된 날 비용 장부가 opus 단가로 과대 계산되지 않게.

**금지 표현은 프롬프트로 막고 `checklist.py` 로 확인합니다.**
프롬프트만으로는 보장이 안 됩니다. 산출물 텍스트를 실제로 검사해 ❌로 올립니다.
점검표는 LLM 을 못 쓴 날에도 항상 나옵니다(❌ 한 줄).

**안전망 실행은 `schedule` 이벤트일 때만 건너뜁니다.**
07:25 크론이 오늘 `data.json` 을 보고 조용히 끝나는데, `workflow_dispatch` 는 항상 돕니다 —
사람이 일부러 다시 돌리는 걸 막으면 안 되니까요.

**네이버 복사 버튼은 실제 브라우저로 검증했습니다 (2026-09-06).**
복사되는 본문에 `<img>` 0, 점선 상자 3, 검색 링크 0, 표 2, 소제목 7 — 미리보기와 링크는 빠지고
서식은 들어갑니다. 스크립트로 누른 클릭은 브라우저가 클립보드 쓰기를 막으므로(사용자 제스처
필요) 자동 테스트로는 확인이 안 됩니다.

**네이버에 붙인 결과를 사용자가 봤습니다 (2026-09-08).** 두 가지가 달랐습니다.
* **문단마다 연한 회색 배경이 깔렸다** → 우리 버그였고 고쳤습니다. Chrome 은 복사할 때 글자
  덩어리마다 **조상(html·body 까지)의 배경색**을 찾아 `background-color` 로 박아 넣습니다.
  맥 클립보드를 직접 읽어 재 보니 문단·제목 18곳에 body 의 `#f5f6f8` 이 실려 있었고, html 바탕을
  바꾸면 그 값이 따라 바뀌었습니다. 이제 html·body·.card 는 투명이고 바탕색은 `body::before`
  층에만 칠합니다. 상자들의 `background-color:#ffffff` 도 뺐습니다(흰 형광펜이 됩니다).
  복사 사본은 언제나 body 바로 아래에 만듭니다. `test_naver_copy_page_paints_no_background_behind_the_text`.
  클립보드 확인법: `osascript -e 'the clipboard as «class HTML»'` 의 16진수를 `xxd -r -p` 로 풀면 됩니다.
* **초록 구분선이 얇은 회색 선이 됐다** → 네이버 쪽입니다. 구분선은 소제목(h2)의 `border-top`
  인데 복사본에는 초록 선이 그대로 들어 있습니다(실측 4곳). 네이버 에디터가 제목의 테두리를
  받지 않고 자기 구분선(회색, 색 선택 없음)으로 바꿉니다. 초록 선을 그대로 넣을 방법은 없어,
  사용자가 **`<hr>` 로 보내기**를 골랐습니다 — `render.section_dividers` 가 둘째 소제목부터 앞에
  `<hr>` 을 넣고, h2 의 border-top 은 뺐습니다. 미리보기의 초록 hr 은 미리보기용이고 네이버에서는
  회색 구분선이 됩니다. `test_naver_html_separates_sections_with_hr_not_heading_borders`.

**수치 강조는 세 단계, 규칙은 `keynumbers.py` 한 곳에 있습니다.**
· 오늘의 핵심 수치(`blog.key_numbers`, 기본 3개) — 브리핑 datapoint 가운데 블로그 본문에 실제로 쓰인 것을
  이슈당 하나씩 고릅니다. 글 맨 위 '오늘의 숫자' 표, 본문 첫 등장 굵은 형광펜, 썸네일 오른쪽 위 배지가
  같은 수치를 씁니다. 본문에 없는 수치는 카드에 올려도 본문에서 못 찾으니 뒤로 미룹니다.
· 되풀이 수치(`blog.highlight_repeats`, 기본 3회) — 첫 등장만 형광펜, 이후는 밑줄. 처음엔 모든 등장에 형광펜을
  칠했더니 하루 글에 30군데가 나와 낮췄습니다.
· 숫자+단위 토큰만 셉니다. '집값' 같은 주제어는 당연히 반복되므로 글자는 세지 않고, 연도(2026년)·날짜(9월 6일)·
  단위 없는 정수도 뺍니다. 소제목 안은 건드리지 않습니다. 인라인 style 을 쓰는 이유는 네이버 에디터가 class 는
  버려도 background-color 는 살리기 때문입니다(붙여넣기 결과는 아직 사람이 안 봤음).

**기사 본문(`output/*/raw/`)은 커밋하지 않습니다 (2026-09-07).**
상위 20건은 본문 전문이 들어 있어 공개 저장소에 매일 올리면 저작권 문제가 됩니다. `.gitignore` 로 막고,
대시보드가 쓰던 건수·피드 상태는 `collect.json` 에 글자 없이 남깁니다. 그래서 `render --date` 재생성은
그 날 수집을 한 컴퓨터에서만 됩니다(Actions 가 만든 날은 불가). 9월 6일치 하나는 이미 이력에 남아 있습니다.

**HTML 은 자동 이스케이프 + `sanitize.clean_html`.**
기사에 심긴 스크립트를 모델이 그대로 옮기면 사이트에서 실행될 수 있어, `.html.j2` 템플릿은 변수를
이스케이프하고(이미 HTML 인 값은 `|safe`), 마크다운→HTML 결과에서 script·on* 속성·javascript: 를 지웁니다.

**품질도 날마다 잽니다 (`state/quality.json`, 2026-09-08).**
비용은 날마다 재면서 품질은 눈으로 보고 흘려보냈습니다. 모델을 값싼 것으로 내리거나 프롬프트를
고친 날 견줄 기준이 없었습니다. 점검표 통과·경고·실패 수와 숫자 검산(확인/미확인)은 이미 만들고
있으니 남기기만 하면 됩니다. `QualityLog.compare()` 가 최근 N일과 그 앞 N일을 견주고, 대시보드에
표로 나옵니다. **모델을 바꾸기 직전에 기준선이 없으면 나중에 비교가 안 됩니다.**
`run` 과 `render` **양쪽 모두** 남깁니다 — 한쪽만 남기면 다시 만든 날이 장부에서 사라집니다.
장부가 없던 날은 사이트를 만들 때 `QualityLog.backfill()` 이 `output/*/checklist.json` 에서
통과·경고·실패만 되살립니다. 검산 건수와 본문 길이는 되살릴 수 없으므로 **0 으로 보여 주지 않고**
표에 '기록 없음' 으로 적고, `compare()` 평균에서도 뺍니다 — 0 이 섞이면 '미확인이 줄었다' 는
거짓말이 됩니다.

**한산한 날은 글을 만들지 않습니다 (`rank.quiet_day`, 2026-09-08).**
기준은 하나입니다 — **여러 매체가 함께 다룬 이야기가 없으면** 조용한 날입니다. 큰 사건이면 반드시
여러 곳이 받아씁니다. 비용보다 큰 이유는 **억지로 채운 잔뉴스 글이 유사문서로 몰리는 걸 막는 것**
입니다. 애매하면 만드는 쪽으로 기웁니다(안 만든 날은 되돌릴 수 없지만 만든 글은 안 올리면 됩니다).
`prompt-pack.md` 는 남습니다. **쉰 날은 `quiet.json` 을 남기고
`failure_streak` 이 실패로 세지 않습니다** — 안 그러면 조용한 이틀에 '2일 연속 실패' 경보가 웁니다.

**글이 언제 기준인지 밝힙니다 (2026-09-08).**
목표가 검색 유입이라 반년 뒤에 들어오는 사람이 있습니다. 그 사람에게는 7월 수치가 '지금 값' 으로
읽힙니다. `render.asof_note()` 가 블로그 맨 위에 한 줄을 넣고(estate-news 의 실거래 각주는 이 저장소에서 돌지 않습니다), 사이트의 30일 지난 글에는 배지가 붙습니다.
`site_page.html.j2` 는 여러 곳에서 쓰이므로 `age_days|default(0, true)` 로 받아야 합니다
(StrictUndefined 라 그냥 쓰면 터집니다).

**접두사 캐싱은 걷어냈습니다 (2026-09-07, 실측으로 확인).**
9월 7일 기록이 **캐시 쓰기 20,814 · 읽기 0** 이었습니다. 원인을 작은 호출 네 번으로 확인했더니
**구조화 출력 스키마가 캐시 접두사에 들어갑니다** — 같은 스키마로 두 번 부르면 8,649토큰이
그대로 읽혔지만 스키마만 바꾸자 읽기 0에 8,705토큰을 통째로 다시 썼습니다. 우리는 브리핑·블로그·
대본의 스키마가 모두 달라 **애초에 공유가 불가능**했습니다. 캐시 쓰기는 입력의 1.25배라, 읽히지
않는 캐시는 웃돈만 무는 셈이었습니다. 다시 켜려면 두 호출이 system 과 **스키마까지** 같아야 합니다.

**브리핑도 `llm.brief_model`(sonnet)로 내렸습니다 (2026-09-07).**
브리핑은 새 글이 아니라 기사를 '정리' 하는 일입니다. 9/6 자료로 실제 호출해 보니 출력이 8,706토큰
이었고, opus 면 약 $0.27, sonnet 이면 $0.106 입니다. 품질이 떨어져 보이면 설정에서 이 줄만 비우세요.

**근거 기사는 주소가 아니라 번호로 오갑니다 (2026-09-07).**
구글뉴스 주소는 한 개가 220자입니다. 프롬프트에 넣고 모델이 그대로 적어 돌려주면 **값을 두 번**
냅니다 — 9월 7일 브리핑 출력의 20%(1,706자)가 주소였습니다. 이제 `format_clusters` 가 기사에
`(1-2)` 같은 번호를 붙이고, 모델은 `source_ids` 만 돌려주며, `pipeline._fill_source_urls` 가
`prompts.article_ids` 로 주소를 되찾습니다. **두 함수는 같은 규칙으로 번호를 매겨야 합니다.**
9/6 자료로 확인했을 때 번호 15개를 모두 알아봤습니다. 번호를 하나도 못 받으면 이미 든 주소를
지우지 않습니다 — 근거가 비면 검산과 '이슈 선정 근거' 가 함께 비기 때문입니다.
공유 문맥(`build_shared_context`)에서도 주소를 빼서 블로그·대본 입력이 함께 줄었습니다.
글 끝의 '참고한 기사' 목록은 원래부터 프로그램이 클러스터에서 만듭니다.

**유튜브 설명란·고정 댓글은 모델이 쓰지 않습니다 (2026-09-07).**
거기 들어갈 것은 챕터 타임코드와 출처 주소뿐이고 둘 다 우리가 가진 값입니다.
`render.youtube_description()` · `render.pinned_comment()` 가 만듭니다. 제목·썸네일 문구 후보도
다섯 개에서 셋으로, B롤은 구간마다 둘까지로 줄였습니다 — 어차피 하나만 고르시니까요.

**비용 한 줄은 실제로 쓴 모델을 적습니다.**
`Usage.summary()` 가 설정값이 아니라 `models_used` 를 보여 줍니다. 안 그러면 브리핑·대본을
값싼 모델로 돌린 날에도 opus 로 돌린 것처럼 보입니다.

**영상 대본만 `llm.script_model`(sonnet)로 돌립니다 (2026-09-07).**
9월 7일 실행이 $0.64 였고 출력 토큰이 대부분이었습니다. 대본은 출력이 가장 큰 호출이라 여기만 값싼 모델로
내렸습니다. 캐시는 모델별로 잡히므로 대본 호출에는 캐시를 걸지 않습니다(안 그러면 캐시 쓰기 값만 더 나감).
어디서 돈이 나가는지 보려고 `Usage.details` → `state/costs.json` 의 `by_call` 에 호출별로 남깁니다.
**고치기 전후 둘 다 실측했습니다.** 2026-09-07 실행이 $0.6441(약 900원, opus 3회),
여섯 가지를 고친 뒤 2026-09-06 을 다시 만든 실행이 **$0.4209(약 590원)** — 35% 줄었습니다.
호출별로는 브리핑 $0.1073(sonnet), 블로그 $0.1520(opus), 영상 대본 $0.1616(sonnet).
가장 비싼 자리는 여전히 블로그이고, 사람이 그대로 올리는 글이라 opus 로 둡니다.

**블로그 글은 짧게, 메인 하나만 (2026-09-07, 사용자 지시).**
이슈 5개를 다 설명해 3천 자가 넘던 것을 1,200~2,000자로 줄였습니다. 메인 이슈 하나만 깊게 쓰고 나머지는
'그 밖의 오늘 소식' 에 한 줄씩. 용어 풀이는 `glossary.py` 의 고정 사전을 씁니다 — 모델이 매번 다르게
설명하면 독자가 헷갈립니다. 'takeaways'(그래서 나는?)는 읽는 사람 유형별 한 줄입니다.

**그림은 공통 카드 틀을 씁니다 (2026-09-07).**
`images.frame_open/frame_close` — 흰 카드·머리말·제목·구분선·각주. 카드 높이는 반드시 `card_height` 로
계산하세요. 고정값으로 두면 각주가 본문과 겹칩니다. 옅은 막대(BLUE_SOFT) 위에는 흰 글씨를 쓰지 마세요.
축 눈금은 `nice_ticks` 를 쓰고, 데이터가 눈금 밖으로 나가지 않는지 확인하세요.

**유입 장치는 넣되 자동 티는 줄입니다 (2026-09-07, 방향 결정).**
사용자가 잡은 방향은 '네이버 블로그 유입 최대화' 입니다. 넣은 것: 대표 검색어(제목 앞 15자·첫 120자·소제목),
3줄 요약, 자동 목차, 마무리 질문, 지난 글 내부 링크, 지난 글과 문장 겹침 검사. 겹침 검사를 넣은 이유는
매일 같은 틀로 쓰는 자동 생성 글이 유사문서로 몰릴 수 있어서입니다. 네이버 순위 기준은 공개되어 있지 않으므로
이 항목들은 경험칙이고, `state/titles.json` 의 조회수 기록이 쌓여야 사실 확인이 됩니다.

**이슈 선정 근거는 주소로 짝짓습니다.**
`explain_issues` 가 이슈와 클러스터를 근거 기사 URL 겹침으로 맞춥니다. 제목 유사도나 순서로 맞추면
모델이 순서를 바꾸거나 제목을 달리 지을 때 엉뚱한 근거가 붙습니다.

**PWA 에 서비스워커를 두지 마세요.**
매일 갱신되는 페이지라 캐시가 남으면 어제 글이 보입니다. manifest 와 아이콘만 둡니다.

**정부 발표 원문은 정책브리핑(korea.kr) 한 곳만 씁니다 (2026-09-07).**
국토교통부 누리집은 TMOSH 쿠키 검사로 프로그램 접속이 막히고(307 무한 이동), korea.kr 의 RSS 는 404 입니다.
그래서 보도자료 **목록 화면을 직접 파싱**합니다. 상세 화면 본문 영역에는 "전재하여 제공" 안내문만 있고 진짜
내용은 문서 뷰어 iframe 안이라, 첨부 PDF 를 받아 `pypdf` 로 글자를 뽑습니다. 120자 미만이거나 '전재하여' 가
든 본문은 버립니다. 거르는 기준은 **키워드 필수 + (부처 또는 강한 제목 키워드)** 입니다 — 부처만 보면 국토부의
항공·철도까지 들어오고, 키워드만 보면 지자체 홍보가 섞입니다. 한 화면이 20건이라 `policy.list_pages`(4장)를
넘깁니다. 같은 문서가 hwp·hwpx·pdf 세 벌로 올라오므로 pdf 우선으로 정렬해 한 개만 받고, 파일명은 한글·영숫자만
남깁니다(괄호·공백이 있으면 마크다운 링크가 깨집니다). 요약은 `llm.policy_model`(haiku)이고, 키가 없으면
□◇ㅇ 표시로 문단을 자르는 `extractive_summary` 로 대신합니다. `doctor` 가 목록 상태를 함께 보여줍니다.

**네이버 검색 노출 확인은 만들었다가 뺐습니다 (2026-09-07, 사용자 결정).**
네이버가 **검색 API 를 개발자센터에서 NAVER API HUB(네이버 클라우드)로 옮겼습니다.** 개발자센터의
애플리케이션 등록 화면에는 이제 로그인·인증서·전자문서·페이·카페·캘린더·캡차 8개만 있고 '검색' 이 없습니다.
HUB 는 지금 무료(하루 25,000회)지만 클라우드 계정과 결제수단이 필요하고 유료 전환이 예고돼 있어,
하루 몇 번 쓰자고 계정을 여는 건 얻는 것보다 번거롭다고 보아 기능을 걷어냈습니다. 다시 만들 일이 생기면
호스트가 `naverapihub.apigw.ntruss.com` 이고 헤더가 `X-NCP-APIGW-API-KEY-ID`/`X-NCP-APIGW-API-KEY` 입니다
(옛 `openapi.naver.com` + `X-Naver-Client-*` 가 아닙니다). 지운 코드는 git 이력의 `indexcheck.py` 에 있습니다.

**공유 카드·피드는 `site.url` 이 있을 때만 만듭니다 (2026-09-07).**
og:image 는 전체 주소만 받고, 크기는 실제 파일 크기를 적습니다(표지 1200×630, 롱폼 썸네일 1280×720).
구독 피드 날짜에 `strftime("%a")` 를 쓰지 마세요 — 언어 설정이 한국어면 한글 요일이 나와 구독기가
못 읽습니다. `_rfc822` 가 요일·달 이름을 직접 적습니다.

**휴대폰(375px)에서 두 곳이 깨져 있었습니다 (2026-09-07에 고침).**
`site_page.html.j2` 에 `img` 규칙이 아예 없어 1000px 그림이 화면을 통째로 밀어냈습니다. 그리고 제작 메모의
'출처' 절에는 링크가 아닌 **맨 주소**가 들어가는데, 구글뉴스 주소 한 덩어리가 1600px 였습니다
(`a { word-break }` 는 `<a>` 에만 걸려 소용없었습니다). `img{max-width:100%}` 와 `p,li{overflow-wrap:anywhere}`
로 고쳤고, 사이트 12쪽을 375px 로 재어 전부 0px 넘침을 확인했습니다. 표는 원래대로 표 안에서만 가로로 움직입니다.

**영상 대본에는 user 프롬프트로만 넘깁니다 (2026-09-07).**
블로그는 프로그램이 값을 넣지만 대본은 모델이 써야 합니다. 캐시 대상인 system 블록에 넣으면 블로그
호출까지 영향을 받으므로 user 쪽에만 붙였습니다. FakeGenerator 의 `generate_video` 서명도 함께
고쳐야 테스트가 통과합니다(실제로 14개가 깨졌었음).

**전체 점검에서 찾아 고친 것 (2026-09-08).**
* **`render` 는 이름과 달리 모델을 다시 부릅니다.** 그림이나 틀만 고쳤을 때 무심코 돌리면
  하루치 값이 그대로 또 나갑니다(실제로 그렇게 $0.42 를 썼습니다). 이제 부르기 전에 최근
  가운뎃값을 원화까지 붙여 알리고 `--no-llm` 을 안내합니다.
* **블로그 분량은 공백을 포함해 셉니다.** 프롬프트가 그렇게 부탁하는데 점검표는 공백을 빼고
  셌습니다. 한국어 본문은 공백이 20%쯤이라, 여유 ±20% 와 정확히 상쇄돼 목표의 3분의 2짜리
  글도 '통과' 로 나왔습니다. 이제 공백 포함, 여유 ±10%.
* **`asof_note` 가 결산 날짜를 사람 말로 옮깁니다.** '2026-W36' 을 그대로 찍어
  "이 글은 2026-W36 기준으로" 라는 문장이 나갔습니다. 이제 '2026년 36주차'.
* **품질 장부를 `render` 경로에도 남깁니다** (위 품질 절 참고).

**깃허브 예약(schedule)은 죽은 게 아니라 몇 시간 늦게 돕니다 — 그래서 맥이 깨웁니다 (2026-09-09 정정).**
2026-09-08 에는 「예약이 통째로 죽어 있다」고 적었지만 틀렸습니다. 그때는 `*/5` 탐침을 걸고
38분만 기다렸는데, 이 계정의 예약 지연은 그보다 깁니다. 실측 —

| 저장소 | cron(UTC) | 실제로 돈 시각(UTC) | 늦음 |
| --- | --- | --- | --- |
| politics-news | 21:45 | 2026-09-08 23:39 | 1시간 55분 (방어막이 18초에 끝냄) |
| estate-news | 21:45 / 22:25 | 2026-09-07 23:47 / 09-08 00:31 | 2시간 2분 / 2시간 6분 |
| rokieyez.github.io | 월 03:17 | 2026-09-07 08:23 | 5시간 6분 |

깃허브 문서는 「부하가 높을 때 schedule 이벤트는 늦어질 수 있다」고만 할 뿐 상한이 없습니다.
계정·저장소 쪽은 다 정상입니다(워크플로 `active`, 공개·비포크·비보관, 기본 브랜치 `main` 에 파일,
cron 마지막 수정자가 rokieyez 계정에 연결됨). **아침 7시를 지키는 건 맥의 launchd** 입니다
(`scripts/wake-daily-brief.sh`, `scripts/net.rokiz.politics-news.daily.plist`, 06:45·07:25). 새 인증키가
필요 없습니다 — `gh` 가 키체인에서 읽고, 빈 환경에서도 되는 것을 확인했습니다.
**깃허브 cron 은 지우지 마세요.** 맥이 꺼져 있던 날엔 두 시간 뒤라도 예약이 대신 만들어 주고,
겹친 날엔 방어막이 30초 안에 끝내서 돈이 안 듭니다. **예약이 안 돈다고 판단하려면 cron 시각에서
최소 6시간은 지나야 합니다.**

**중복 실행 방어막은 모든 경로에 걸립니다 (2026-09-08).**
예전에는 `event_name == "schedule"` 일 때만 걸렸습니다. 밖에서 깨우면 전부
`workflow_dispatch` 라 방어막이 없는 셈이었고, 깃허브 cron 이 되살아나는 날 하루 두 번
돌고 돈도 두 번($0.84) 나갔을 것입니다. 지금은 **날짜를 손으로 지정한 실행만** 건너뜁니다
(일부러 다시 만드는 경우). 실제로 재 봤습니다 — 겹친 실행은 24초에 7단계를 건너뛰고
모델을 부르지 않습니다.

**블로그 글을 눈에 들어오게 간결히 (2026-09-08, 사용자 요청).**
발행된 글이 "지저분하고 복잡하다" 는 지적을 받아 재 봤더니 근거가 나왔습니다 —
본문 5,058자에 **강조 42회**(굵게 25·밑줄 11·형광 6), **배경 상자 10개**, 그리고
**첫 문단에 닿기까지 상자 6개·444자**. 게다가 `29.5%` 가 아홉 번 나왔습니다.
머리에 3줄 요약·이 글의 순서·낯선 말 풀이·오늘의 숫자가 연달아 서서 같은 수치를
본문 전에 세 번 말해 버리고 있었습니다.

고친 것 다섯 가지:
* 머리에는 **3줄 요약 하나만**. 목차·낯선 말 풀이·오늘의 숫자 카드를 뺐습니다.
  (낯선 말은 본문이 이미 괄호로 설명하고 있어 겹쳤습니다.)
* 되풀이 수치의 **밑줄을 없앴습니다.** 형광펜은 첫 등장에만, 두 번째부터는 맨몸.
* 프롬프트에 "같은 수치를 되풀이하지 말고 뒤에서는 가리키기만" 을 넣었습니다.

결과: 상자 10 → 7개, 강조 42 → 22회, 첫 문단까지 6개·444자 → **1개·152자**.
`lead_block_html` 의 `outline` 과 `to_naver_html` 의 `terms`·`key_numbers` 인자는
부르는 쪽 호환을 위해 남겨 두었지만 더 이상 화면에 그리지 않습니다.

**카드뉴스 — 유튜브 게시물용 (2026-09-08, 사용자 요청).**
`images.cards()` 가 하루치 브리핑을 정사각 1080×1080 카드 5~7장으로 나눠 담습니다.
구성은 표지 → (표결) → 오늘의 숫자 → 이슈 2~3장 → 그 밖의 소식. **「내일 볼 것」 카드는
2026-09-09 사용자 지시로 뺐습니다** — 내일 볼 것은 블로그·대본에만 남습니다.
**모델을 새로 부르지 않습니다** — 이미 만든 `DailyBrief` 를 나눌 뿐이라 하루 비용이
그대로입니다(시험으로 호출 3회가 유지되는지 지킵니다).
PNG 는 **1배(1080×1080)** 로 뽑습니다(`images.png_scale_cards: 1`, 2026-09-08 사용자 요청). 그전엔
본문 그림과 같은 2배(2160)라 하루 6.4MB 가 쌓였습니다. 썸네일·본문 그림은 여전히 2배입니다.

* **정사각인 이유**: 유튜브 게시물은 세로 이미지를 잘라 보여 주는 화면이 있습니다.
* **세로 가운데 정렬**: 내용을 먼저 재고 가운데에 놓습니다. 위에서부터 쌓기만 하면
  자료가 적은 날 카드 아래 절반이 텅 빈 채로 남아 '덜 만든 것' 처럼 보입니다.
* **배지는 제목에 실제로 나온 수치만**. 값만 견주면 `6` 이 `162만원` 안의 6 에 걸려
  제목은 분당 집값인데 배지는 '6억원 이하' 가 뜹니다 (실제로 그랬습니다).
* **부제는 문장 단위로 자릅니다** (`_sentence_fit`). 줄 수로만 자르면 "…수도권 전반의"
  처럼 말이 끊긴 채 박힙니다.
* `data.json` 에는 `what_happened` 가 없어 사이트 자료로 다시 그리면 불릿이 빠집니다.
  파이프라인에서는 온전한 브리핑이 들어오므로 채워집니다.

끄려면 `images.cards: false`.

**장수는 고정이 아닙니다 (2026-09-08, 사용자 지시).** 그날 이슈가 몇 건이냐에 따라
네댓 장에서 열 장까지 늘고 줍니다. `images.cards_max`(기본 10)는 **상한일 뿐 목표가
아닙니다** — 억지로 채우면 내용 없는 카드가 한 장 더 붙습니다.

* 셈법은 `표지 + (표결) + (수치 2개 이상이면 숫자) + 이슈 + (남은 이슈가 있으면 그 밖의 소식)`
  입니다. 이슈 카드 수가 상한에 맞춰 정해집니다.
* **표지 제목의 줄은 기사 사이(·)에서만 바뀝니다** (`images._fit_units`, 2026-09-10 사용자 지시). 기본 크기에서
  기사들을 채워 넣고, 어느 기사가 한 줄에 안 들어가면 그때만 글씨를 줄입니다. 하한에서도 안 들어가는 기사는
  그 기사만 접되 다른 기사와 한 줄을 나누지 않습니다. 그전엔 「…김승원 / 공방·김민석…」으로 잘리고 마지막
  줄만 금색이라 한 기사가 두 기사처럼 보였습니다. 숫자 사이 가운뎃점(1·2위)은 나누지 않습니다.
* **한 건짜리 '그 밖의 소식' 카드는 만들지 않습니다** — 그 한 건에게 제 카드를 줍니다.
  한 줄만 적힌 목록 카드는 자리만 차지합니다.
* 실측(2026-09-08): 이슈 1건 → 3장, 3건 → 6장, 5건 → 8장, 7건 이상 → 10장.
  `test_card_count_follows_the_day` 가 지킵니다.

**되풀이를 세 곳에서 끊었습니다 (2026-09-08, 사용자 지시).**
카드 여덟 장의 글자를 뽑아 재 보니 낱말이 이만큼 겹쳤습니다 — 이슈 4↔7 **67%**,
숫자↔이슈3 **54%**, 숫자↔이슈4 35%, 표지↔이슈5 39%. `29.5%` 가 네 장에 나왔습니다.

* **숫자 카드는 이슈 카드가 말하지 않는 수치만 담습니다.** 겹칠 것이 없으면 한 장을
  통째로 뺍니다(오늘이 그런 날이라 8장 → 7장). 예전에는 이슈 카드 배지의 예고편이라
  글자까지 똑같았습니다.
* **표지 부제를 없앴습니다.** 부제가 3·4·5번 내용을 앞당겨 말해 뒤 카드가 되풀이처럼
  읽혔습니다. 표지는 대문 + 제목 하나입니다.
* **같은 사건이 갈린 이슈는 카드에서 뺍니다** (`_drop_repeats`, 기준 0.40).
  **브리핑·블로그는 건드리지 않습니다** — 카드만 거릅니다. 기준은 재서 골랐습니다:
  그날 열 쌍 중 겹친 한 쌍이 0.51, 나머지 아홉 쌍이 0.03~0.16 이라 그 사이를 끊었습니다.
  근본 해결은 클러스터·프롬프트 쪽이라 따로 봐야 합니다.

고친 뒤 남은 겹침은 표지↔이슈 35~38% 뿐이고, 이건 표지가 그날 대표 이슈를 제목에
쓰기 때문이라 없앨 수 없습니다.

**고아 낱말은 조금 줄여 끌어올립니다 (2026-09-08, 사용자 지적).** 「…아파트 / 실종」처럼
마지막 줄에 낱말 하나만 떨어지면 제목이 두 동강 나 보입니다. `_fit` 이 10% 안쪽에서
글씨를 줄여 한 줄로 붙입니다 — **하한(floor)은 넘지 않습니다.** 넘게 두었더니
'오늘의 숫자' 설명이 32px 아래 28px 까지 내려갔습니다. 이슈 카드 글자 폭도
888 → 904 로 넓혔습니다(오른쪽 여백 96 → 80).
곁들여 알아 둘 것 — `_title_block` 은 **제목이 두 줄 이상일 때만** 마지막 줄에
신호색을 얹습니다. 낱말을 골라 칠하는 게 아니라서 제목이 한 줄이면 강조색이
없습니다. 사용자가 그 상태로 두기로 했습니다.

**표지는 언제나 사진 대문입니다 (2026-09-08, 사용자 지시).**
사진 위에 검정 55% 를 덮고 가운데에 날짜(고정폭)와 **정치 주요이슈**(92px 흰 글씨)를
얹습니다. **누르지 않으면 밝은 하늘이나 흰 건물 위에서 흰 글씨가 사라집니다.**
아래 판에는 그날의 제목만 들어갑니다.

**내려받기 봉투는 둘입니다 (2026-09-08, 사용자 요청).**
`<날짜>_blogfiles.zip`(그림·자막·컷 리스트·보도자료 원본)과 `<날짜>_card_news.zip`(카드뉴스).
가는 곳이 다릅니다 — 첨부물은 네이버 글에, 카드는 유튜브 게시물에 올립니다. 한 봉투면
유튜브에 올릴 때마다 필요 없는 자막·컷 리스트를 골라내야 합니다. 패턴은 `site.CARD_GLOBS`.
카드가 없는 날엔 빈 봉투를 만들지 않습니다.

**카드 디자인은 「청사진」 판입니다 (2026-09-08, 시안 22개에서 사용자가 고름).**
시각 철학은 `docs/카드뉴스-디자인-철학.md` 에 있고, 카드를 고치기 전에 그걸 먼저 보세요.

* **낯은 셋뿐**입니다 — `CARD_DARK`(표지·목록) · `CARD_FLOOD`(숫자) · `CARD_PAPER`(이슈).
  각각 (바탕, 글씨, 낮은 글씨, 선, 신호색) 다섯 색이고 `CARD_FACES` 가 카드 종류에 짝지어 줍니다.
* **색은 재서 정합니다.** 작은 글씨는 모두 대비 4.5 이상. 눈으로는 멀쩡해 보이는데 걸린 적이
  네 번 있습니다(주황 판 2.78·2.75, 청사진 판 4.29·3.72).
  `test_card_palette_keeps_readable_contrast` 가 세 낯을 전부 잽니다.
* **글꼴은 프리텐다드 + IBM Plex Mono 이고, 쓴 글자만 잘라 심습니다** (2026-09-08,
  사용자 요청). 한글은 `Pretendard-{Regular,Bold}.otf`, 숫자·기호는 IBM Plex Mono 입니다.
  **통째로 심으면 안 됩니다** — 프리텐다드 한 벌이 1.5MB 라 두 굵기를 그대로 넣으면 카드
  한 장이 4MB 를 넘습니다. `fontTools` 로 그 카드에 쓴 글자만 잘라내면 네 벌 합쳐 23KB 입니다
  (실측). `fontTools` 가 없으면 조용히 시스템 글꼴로 갑니다 — 러너의 `fonts-noto-cjk` 설치
  단계를 남겨 둔 이유가 그 대비입니다. 자세한 건 `assets/fonts/README.md`.
* **글꼴 자리표를 먼저 넣고 마지막에 바꿉니다** (`_FONT_CSS_TOKEN` → `_embed_fonts`).
  글꼴을 SVG 머리에 넣는데 어떤 글자를 쓸지는 다 그려 봐야 알기 때문입니다.
* **카드 글꼴은 카드에만 씁니다** (`CARD_FONT`). 블로그 인포그래픽까지 바꾸면 러너에
  프리텐다드가 없어 맥에서 만든 그림과 러너에서 만든 그림이 달라집니다.
* **글자 크기는 2026-09-08 에 몇 번 손봤습니다** (사용자가 화면에 상자를 쳐서 지정).
  최종은 이렇습니다 — **읽는 글자는 크게, 계측 표식은 작게.**

  | 무엇 | 크기 | 왜 |
  | --- | --- | --- |
  | 표지 제목 | 104 (그림 있으면 80) | 화면을 지배한다 |
  | 소제목(오늘의 숫자·그 밖의 소식) | **80** | 표지 제목과 같은 무게로 |
  | 큰 숫자 | 140 | |
  | 본문 요약 | 46 (그림 있으면 43), 표지 부제 44 | 실제로 읽는 글 |
  | 목록 항목 | 58/54/48/42 (항목 수에 따라) | |
  | 수치 설명·이슈 분류 | 46 / 39 | |
  | 머리글·날짜·쪽번호·사진 출처 | **28 / 26 / 28 / 22** | 계측 표식이라 작아야 표식이다 |

  머리글을 42 로 키웠다가 되돌렸습니다 — 사용자가 "빨간 상자 안은 원래대로" 로 지정.
  더 키우려면 `_fit` 의 `floor` 도 함께 올려야 합니다 — 안 그러면 긴 제목만 예전
  크기로 줄어들어 카드마다 크기가 들쭉날쭉해집니다.
* **줄 수를 안 늘리면 글씨를 키워도 소용없습니다.** 표지 부제를 36→44 로 올렸는데도
  그대로였습니다. `_sentence_fit` 이 두 줄 안에 넣으려고 도로 36 으로 줄였기 때문입니다.
  최대 줄 수를 3 으로 늘리고 나서야 커졌습니다.
* **'오늘의 숫자' 설명은 한 줄로 놓고 넓게 씁니다** (`label_w = w - 170`, 2026-09-08
  사용자 제안). 두 줄을 허용하면 한 줄당 60px 씩 먹어 셋째 수치가 밀려납니다. 한 줄로
  두면 글씨는 조금 작아져도(46 → 38쯤) **셋이 다 올라갑니다.** 다만 `_fit` 은 하한에
  닿으면 말을 잘라 버리므로, 한 줄에 도저히 안 들어가는 긴 설명은 두 줄로 풀어 줍니다 —
  잘린 설명보다 낫습니다. 그러고도 넘치면 수치를 112 까지 줄이고, 그래도 안 되면 덜어 냅니다.
* **쪽번호는 h-80 입니다.** 안쪽 테두리 아래 변이 h-58 인데 h-60 에 두었더니 글자
  아랫부분이 선과 겹쳤습니다. `test_page_number_stays_inside_the_inner_border` 가 지킵니다.
* **이슈 카드가 넘칠 때는 뒤에서부터 덜어 내면 안 됩니다.** 한 줄 요약이 통째로 빠져
  제목만 남은 적이 있습니다. 차례는 ① 덧붙인 사실 줄 ② 요약 글씨 줄이기 ③ 그래도 안 되면
  덜어 내기 입니다.
* **고정폭은 반드시 조여야 합니다** (`_numeral`). 자간·낱말 사이·마침표 앞뒤 세 곳입니다.
  낱말 사이를 안 줄이면 '6억원 이하' 의 빈칸이 76px 을 먹고, 마침표를 안 당기면 `29.5%` 가
  `29 . 5%` 로 읽힙니다. 둘 다 실제로 그렇게 나왔습니다.

**카드는 위 그림 · 아래 글 두 층입니다 (2026-09-08, 사용자가 예시를 주고 요청).**
위쪽 470px 띠에 그림, 아래 판에 청사진 글. 그림이 없으면 예전처럼 화면 전체가 글자 카드가
되고, 둘이 섞여 일곱 장에 박자를 만듭니다. 켜고 끄기는 `images.cards_art`.

* **그림은 세 갈래입니다** (`Renderer._card_art`). ① 그날 폴더에 손으로 넣은 `photo-1.jpg`
  ② 무료 사진(Pexels, `PEXELS_API_KEY`) ③ 그날 그린 인포그래픽(`img-*.png`).
  사진 장수는 **표지 1 + 이슈 카드 수**로 그날 정해집니다(2026-09-09, 사용자 선택 ⑤). 그전엔 상한 4장이라
  이슈 카드 5·6번이 글자만 있었습니다. `images.photos_max`(10)는 상한일 뿐입니다.
  **기사 사진은 쓰지 않습니다** — 저작권이 있고 애초에 수집하지도 않습니다.
* **띠는 가득 채웁니다**(`slice`). 담아서 넣으면 양옆이 허옇게 비어 '덜 만든 것' 처럼 보입니다.
  대신 세로로 긴 인포그래픽(비율 1.35 미만, 자치구 지도 등)은 잘리면 축·범례가 날아가므로
  아예 쓰지 않습니다. 사진은 잘려도 되니 이 검사에서 뺍니다.
* **더 당겨 자르지 마세요.** 인포그래픽의 흰 테두리와 자기 날짜 줄을 밀어내려고 1.34배로
  당겨 봤는데 수치까지 잘려 `162만원` 이 `62만원` 이 됐습니다. 있는 그대로 채웁니다.
* **띠 높이는 470 이 하한입니다.** 548 로 잡았더니 아래 판이 348px 밖에 안 남아 이슈 카드의
  한 줄 요약이 통째로 잘렸습니다. `test_cards_put_the_graphic_on_top_and_the_words_below`
  가 이걸 지킵니다.
* 제목은 **마지막 줄만 신호색**입니다(`_title_block`). 한 덩이를 통째로 흰 글씨로 두면
  어디서 눈이 멎어야 할지 알 수 없습니다.

**무료 사진은 Pexels 입니다 (2026-09-08, 사용자 선택). `rebrief/photos.py`.**
인증키가 없으면 조용히 건너뛰고 인포그래픽으로 갑니다 — **사진은 있으면 좋은 것이지 없으면
안 되는 것이 아닙니다.** 망·인증키·응답 형식 무엇이 터져도 로그만 남기고 빈 목록을 냅니다.

* **왜 Pexels 인가.** 인증키 없이 되는 Openverse 도 실제로 호출해 봤습니다 — '서울 아파트' 로(estate-news 에서)
  상업 이용이 허락된 사진이 **일곱 장**(동일조건변경허락까지 넣어도 27장)뿐이라 며칠이면 같은
  사진이 돌아옵니다. Pexels 는 이메일만 넣으면 심사 없이 키가 나오고 표기 의무도 없습니다.
* **'본문과 무관' 을 절대 빼지 마세요.** 은마아파트 기사 옆에 아무 아파트 사진이 붙으면 읽는
  사람은 그게 은마인 줄 압니다. 사진 위 띠에 「사진 촬영자 / Pexels · 본문과 무관」 을 답니다.
  `test_cards_credit_the_photo_and_say_it_is_unrelated` 가 지킵니다.
* **받은 사진은 커밋하지 않습니다.** 한 장이 3~6MB 라 매일 올리면 한 해에 1.8GB 씩 붑니다.
  `state/photos/` 는 gitignore 이고, 쓴 사진 번호만 `state/photos.json` 에 남겨 어제 그
  사진이 오늘 또 나오지 않게 합니다(최근 40장).
* **찾을 말은 그날 이슈에서 뽑습니다** (`photos.QUERY_MAP`). 매일 같은 낱말로 찾으면 매일 같은
  사진이 옵니다. 국회→국회의사당, 선거→투표, 수사→법원 식입니다. **영어로 찾습니다** — 한국어로
  넣으면 몇 건 안 나옵니다.
* **시험은 망을 타면 안 됩니다.** `tests/conftest.py` 가 `images.photos = False` 로 끕니다.
  이 줄을 지우면 `.env` 에 키를 넣은 컴퓨터에서 시험이 조용히 인터넷을 쓰기 시작합니다.

## 운영 상태

- 2026-09-08 저장소 생성·푸시. Pages 켬. Secrets 는 같은 날 사용자가 넣었고 첫 실행(브리핑 2026-09-08)이 성공. 맥 launchd 깨우미(`net.rokiz.politics-news.daily`)도 같은 날 설치.
- 2026-09-09 둘째 실행 성공 — launchd `workflow_dispatch` 07:08·07:25 두 번(두 번째는 방어막). 깃허브 `schedule` 도 08:39 KST 에 뒤늦게 와서 방어막이 18초에 끝냄. 카드 PNG 는 1080×1080 으로 나옴.
- 2026-09-09 마무리 회차 2(렌즈: 오늘 산출물과 운영 이음새): 데일리는 $0.448·3회 호출. civics 는 발의법률안·본회의·법안 추적·정책 원문 넷 다 ConnectTimeout(위 '접속' 문단). 중복 방어막이 체크아웃이 아니라 가지 끝을 보게 고침(estate-news 경주 사례).
  카드 묶음 zip 7장 전부 1080. 네이버 글에 `body::before`·`<hr>` 반영 확인. 사이트 링크는 `latest/brief.html` 식이며 `latest/` 자체는 404(정상).
- 2026-09-08 마무리 점검: `IssueBrief.category` 스키마가 아직 부동산 분류(가격동향·청약·분양…)를 모델에 주고 있었음 → 정치 분류 7가지로. RSS 설명·브리핑의 '시장 온도' 표기·`who_is_affected` 예시도 정치용으로. 모델은 그 전에도 '국회·입법' 으로 답했지만 스키마와 프롬프트가 어긋난 채였음.
- 워크플로 파일은 estate-news 것을 이름만 바꿔 가져왔습니다(정치 데일리 브리핑·주간·월간·피드 점검·
  테스트·사이트 배포·제목 기록·발행 기록). 저장소를 만들면 그대로 돕니다.
- RSS 피드: 2026-09-08 doctor 기준 **18개 중 17개 정상**. 중앙일보만 항목이 비어 꺼 둠.
  한 번 돌리면 900건 넘게 들어옵니다(연합 120·뉴시스 100·조선 100·구글뉴스 정치 100…).
- 정책브리핑 최근 20건 중 정치 관련 3건 (doctor 확인).
- 모델·비용 설정은 estate-news 와 같습니다(블로그만 opus, 브리핑·대본 sonnet). 실측 비용은 아직 없음.
- `doctor` 의 '선택 기능 인증키' 에 실거래가·부동산원이 아직 나옵니다 — 복사한 `.env` 에 키가 있어서이고,
  이 저장소에서는 쓰지 않습니다(`stats.enabled: false`).

## 문서

- `README.md` — 비개발자용. 설정 4단계, 매일 할 일, 문제 해결.
- `docs/기술문서.md` — 파이프라인 내부, 명령어, 폴더 구조. 2026-09-08 정치판으로 다시 씀(통계 절 → civics 절)
- `docs/카드뉴스-디자인-철학.md` — 카드 디자인 철학. 카드를 고치기 전에 먼저 볼 것.

## 작업할 때

- 테스트는 네트워크 없이 픽스처로 전 구간을 돕니다: `python -m pytest tests/ -q`
- 템플릿은 `StrictUndefined` 라 변수명을 틀리면 테스트에서 바로 잡힙니다.
- `.env` 는 절대 읽거나 출력하지 않습니다. 인증키를 파일에 적지 않습니다.
- 기사 본문(`output/**/raw/`)은 공개 저장소에 올리지 않습니다.
